% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{compare_restrictions}
\alias{compare_restrictions}
\title{Compares posterior restriction probabilities across model specifications}
\usage{
compare_restrictions(
  ...,
  restrictions = NULL,
  zero_tol = 1e-08,
  probability = 0.9
)
}
\arguments{
\item{...}{Posterior model objects or a named list of model objects.}

\item{restrictions}{Optional list of restriction specifications evaluated for
each model.}

\item{zero_tol}{Numerical tolerance for zero restrictions.}

\item{probability}{Probability mass of the equal-tailed credible intervals
reported in posterior summaries.}
}
\value{
A \code{bsvar_post_tbl} containing posterior restriction
probabilities across models, with a \code{model} column identifying each
specification.
}
\description{
Evaluates the same identifying restrictions for two or more posterior model
objects and combines their posterior probabilities in a common table.
}
\examples{
data(us_fiscal_lsuw, package = "bsvars")
spec <- bsvars::specify_bsvar$new(us_fiscal_lsuw, p = 1)
post1 <- bsvars::estimate(spec, S = 5, show_progress = FALSE)
post2 <- bsvars::estimate(spec, S = 5, show_progress = FALSE)

r <- list(irf_restriction("gdp", "gdp", 0, sign = 1))
comp <- compare_restrictions(m1 = post1, m2 = post2, restrictions = r)
head(comp)
}
