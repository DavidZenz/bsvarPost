% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-joint-inference.R
\name{plot_simultaneous}
\alias{plot_simultaneous}
\title{Plot simultaneous posterior bands}
\usage{
plot_simultaneous(
  object,
  type = c("irf", "cdm"),
  horizon = NULL,
  probability = 0.9,
  variable = NULL,
  shock = NULL,
  models = NULL,
  facet_scales = "free_y",
  scale_by = c("none", "shock_sd"),
  scale_var = NULL,
  ...
)
}
\arguments{
\item{object}{A simultaneous-band table or an object accepted by
\code{simultaneous_irf()} / \code{simultaneous_cdm()}.}

\item{type}{One of \code{"irf"} or \code{"cdm"} when \code{object} is not already a
simultaneous-band table.}

\item{horizon}{Maximum horizon used when \code{object} is a posterior model
object.}

\item{probability}{Coverage probability for the simultaneous band.}

\item{variable}{Optional response-variable subset.}

\item{shock}{Optional shock subset.}

\item{models}{Optional model filter.}

\item{facet_scales}{Facet scales passed to \code{ggplot2}.}

\item{scale_by}{Optional scaling mode for CDMs.}

\item{scale_var}{Optional scaling variable specification.}

\item{...}{Additional arguments passed to computation methods.}
}
\value{
A \code{ggplot} object.
}
\description{
Plot simultaneous posterior bands
}
\examples{
data(us_fiscal_lsuw, package = "bsvars")
spec <- bsvars::specify_bsvar$new(us_fiscal_lsuw, p = 1)
post <- bsvars::estimate(spec, S = 5, show_progress = FALSE)

p <- plot_simultaneous(post, horizon = 3)
}
