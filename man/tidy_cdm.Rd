% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{tidy_cdm}
\alias{tidy_cdm}
\alias{tidy_cdm.default}
\alias{tidy_cdm.PosteriorBSVAR}
\title{Summarises posterior draws of cumulative dynamic multipliers}
\usage{
tidy_cdm(object, ...)

\method{tidy_cdm}{default}(object, ...)

\method{tidy_cdm}{PosteriorBSVAR}(
  object,
  horizon = NULL,
  probability = 0.9,
  draws = FALSE,
  model = "model1",
  scale_by = c("none", "shock_sd"),
  scale_var = NULL,
  ...
)
}
\arguments{
\item{object}{A posterior model object or a \code{PosteriorCDM} array.}

\item{...}{Additional arguments passed to computation methods.}

\item{horizon}{Forecast horizon when \code{object} is a posterior model object.}

\item{probability}{Probability mass of the equal-tailed credible interval.}

\item{draws}{If \code{TRUE}, report individual posterior draws rather than
posterior summaries.}

\item{model}{Label identifying the model specification.}

\item{scale_by}{Optional scaling mode for CDMs.}

\item{scale_var}{Optional scaling variable specification.}
}
\value{
A \code{bsvar_post_tbl} (tibble subclass) with columns \code{model},
\code{object_type}, \code{variable}, \code{shock}, \code{horizon},
\code{mean}, \code{median}, \code{sd}, \code{lower}, and
\code{upper}.  When \code{draws = TRUE}, columns \code{draw} and
\code{value} replace the summary statistics.
}
\description{
Transforms posterior draws of cumulative dynamic multipliers into a table
containing posterior means, medians, standard deviations, and equal-tailed
credible intervals, or retains the individual draws when requested.
}
\examples{
# Small posterior (S = 5 draws)
data(us_fiscal_lsuw, package = "bsvars")
spec <- bsvars::specify_bsvar$new(us_fiscal_lsuw, p = 1)
post <- bsvars::estimate(spec, S = 5, show_progress = FALSE)

# Posterior summaries of cumulative dynamic multipliers
result <- tidy_cdm(post, horizon = 3)
head(result)
}
