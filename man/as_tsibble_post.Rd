% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tsibble.R
\name{as_tsibble_post}
\alias{as_tsibble_post}
\title{Convert bsvarPost tidy outputs to tsibble}
\usage{
as_tsibble_post(
  object,
  key = NULL,
  index = NULL,
  regular = NULL,
  validate = TRUE
)
}
\arguments{
\item{object}{A \code{bsvar_post_tbl}.}

\item{key}{Optional key columns. By default, \code{bsvarPost} chooses a key from
\code{model}, \code{variable}, \code{shock}, and \code{draw} when present.}

\item{index}{Optional index column. By default, \code{horizon} is used if present,
otherwise \code{time}.}

\item{regular}{Optional regularity flag passed to \code{tsibble::as_tsibble()}.}

\item{validate}{Passed to \code{tsibble::as_tsibble()}.}
}
\value{
A \code{tsibble::tbl_ts} object.
}
\description{
Convert bsvarPost tidy outputs to tsibble
}
\examples{
\dontrun{
data(us_fiscal_lsuw, package = "bsvars")
spec <- bsvars::specify_bsvar$new(us_fiscal_lsuw, p = 1)
post <- bsvars::estimate(spec, S = 5, show_progress = FALSE)

irf_tbl <- tidy_irf(post, horizon = 3)
ts_tbl <- as_tsibble_post(irf_tbl)
print(ts_tbl)
}
}
