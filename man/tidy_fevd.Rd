% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{tidy_fevd}
\alias{tidy_fevd}
\alias{tidy_fevd.default}
\title{Summarises posterior draws of forecast error variance decompositions}
\usage{
tidy_fevd(object, ...)

\method{tidy_fevd}{default}(object, ...)
}
\arguments{
\item{object}{A posterior model object or a \code{PosteriorFEVD} array.}

\item{...}{Additional arguments passed to computation methods.}
}
\value{
A \code{bsvar_post_tbl} (tibble subclass) with columns \code{model},
\code{object_type}, \code{variable}, \code{shock}, \code{horizon},
\code{mean}, \code{median}, \code{sd}, \code{lower}, and
\code{upper}.  When \code{draws = TRUE}, columns \code{draw} and
\code{value} replace the summary statistics.
}
\description{
Transforms posterior draws of forecast error variance decompositions into a
table containing posterior means, medians, standard deviations, and
equal-tailed credible intervals, or retains the individual draws when
requested.
}
\examples{
# Small posterior (S = 5 draws)
data(us_fiscal_lsuw, package = "bsvars")
spec <- bsvars::specify_bsvar$new(us_fiscal_lsuw, p = 1)
post <- bsvars::estimate(spec, S = 5, show_progress = FALSE)

# Posterior summaries of forecast error variance decompositions
result <- tidy_fevd(post, horizon = 3)
head(result)
}
