% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-event.R
\name{plot_shock_ranking}
\alias{plot_shock_ranking}
\title{Plots ranked shock contributions over selected historical periods}
\usage{
plot_shock_ranking(
  object,
  start = NULL,
  end = start,
  variables = NULL,
  models = NULL,
  ranking = c("absolute", "signed"),
  top_n = NULL,
  probability = 0.9,
  ...
)
}
\arguments{
\item{object}{A posterior model object, \code{PosteriorHD}, or a table of ranked
structural-shock contributions.}

\item{start}{First time index to include when \code{object} is not already a shock
ranking table.}

\item{end}{Last time index to include. Defaults to \code{start}.}

\item{variables}{Variables whose historical decompositions are included.}

\item{models}{Model specifications to include.}

\item{ranking}{One of \code{"absolute"} or \code{"signed"}.}

\item{top_n}{Optional number of top-ranked shocks to keep per model-variable panel.}

\item{probability}{Probability mass of the equal-tailed credible interval.}

\item{...}{Additional arguments passed to \code{shock_ranking()}.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots structural shocks ordered by the absolute or signed magnitude of their
contributions over a selected historical period.
}
\examples{
data(us_fiscal_lsuw, package = "bsvars")
spec <- bsvars::specify_bsvar$new(us_fiscal_lsuw, p = 1)
post <- bsvars::estimate(spec, S = 5, show_progress = FALSE)

p <- plot_shock_ranking(post, start = "1948.25", end = "1948.5")
}
