---
phase: 06-api-consistency-error-messages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/validation.R
  - R/utils.R
  - R/compare.R
autonomous: true

must_haves:
  truths:
    - "deprecate_arg() helper exists and issues warning when old param used"
    - "resolve_horizon() converts NULL to integer 20L via validate_horizon()"
    - "validate_horizon() errors include actual received value"
    - "validate_probability() errors include actual received value"
    - "set_response_dimnames() errors on non-4D input"
    - "set_time_dimnames() errors on non-3D input"
    - "set_hd_dimnames() errors on non-4D input"
    - "new_bsvar_post_tbl() errors when required columns missing"
    - "compare_* functions validate model variable name compatibility before rbind"
    - "compare_* functions validate horizon range compatibility before rbind"
    - "compare_irf/cdm/fevd/forecast/hd_event/restrictions default to probability=0.90"
    - "compare_irf/cdm/fevd/forecast default to horizon=NULL"
  artifacts:
    - path: "R/validation.R"
      provides: "deprecate_arg() and resolve_horizon() helpers"
      contains: "deprecate_arg"
    - path: "R/utils.R"
      provides: "Dimension assertions in set_*_dimnames, column validation in new_bsvar_post_tbl"
      contains: "expected a 4-dimensional array"
    - path: "R/compare.R"
      provides: "Model compatibility validation + updated defaults"
      contains: "validate_model_compatibility"
  key_links:
    - from: "R/compare.R"
      to: "R/utils.R"
      via: "validate_model_compatibility called before do.call(rbind)"
      pattern: "validate_model_compatibility"
    - from: "R/compare.R"
      to: "R/validation.R"
      via: "resolve_horizon used for horizon=NULL default"
      pattern: "resolve_horizon"
---

<objective>
Add validation infrastructure and update defaults in validation.R, utils.R, and compare.R.

Purpose: Establish the helpers (deprecate_arg, resolve_horizon), structural validators (dimension assertions, column validation, model compatibility), and update compare_* defaults to probability=0.90 and horizon=NULL. These are the foundational changes that all other files depend on.

Output: Three updated R source files with new helpers, validators, and corrected defaults.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-api-consistency-error-messages/06-CONTEXT.md
@.planning/phases/06-api-consistency-error-messages/06-RESEARCH.md
@R/validation.R
@R/utils.R
@R/compare.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deprecate_arg(), resolve_horizon(), and "Received" values to validation.R</name>
  <files>R/validation.R</files>
  <action>
**Part A: Update existing validators to include actual received values in error messages (per locked user decision: "Validation errors must include actual values (expected vs received)").**

In `validate_horizon()`, update each `stop()` call to append the actual received value:
- `"'", arg_name, "' cannot be NULL."` becomes `"'", arg_name, "' cannot be NULL. Received: NULL"`
- `"'", arg_name, "' must be a single numeric value."` becomes `"'", arg_name, "' must be a single numeric value. Received: ", deparse(horizon)`
- `"'", arg_name, "' must be non-missing (not NA)."` becomes `"'", arg_name, "' must be non-missing (not NA). Received: NA"`
- `"'", arg_name, "' must be finite (not Inf)."` becomes `"'", arg_name, "' must be finite (not Inf). Received: ", horizon`
- `"'", arg_name, "' must be non-negative."` becomes `"'", arg_name, "' must be non-negative. Received: ", horizon`
- `"'", arg_name, "' must be positive (> 0)."` becomes `"'", arg_name, "' must be positive (> 0). Received: ", horizon`
- `"'", arg_name, "' must be an integer value."` becomes `"'", arg_name, "' must be an integer value. Received: ", horizon`

In `validate_probability()`, update each `stop()` call similarly:
- `"'", arg_name, "' cannot be NULL."` becomes `"'", arg_name, "' cannot be NULL. Received: NULL"`
- `"'", arg_name, "' must be a single numeric value."` becomes `"'", arg_name, "' must be a single numeric value. Received: ", deparse(probability)`
- `"'", arg_name, "' must be non-missing (not NA)."` becomes `"'", arg_name, "' must be non-missing (not NA). Received: NA"`
- `"'", arg_name, "' must be finite (not Inf)."` becomes `"'", arg_name, "' must be finite (not Inf). Received: ", probability`
- `"'", arg_name, "' must be strictly between 0 and 1 (exclusive)."` becomes `"'", arg_name, "' must be strictly between 0 and 1 (exclusive). Received: ", probability`

Use `deparse()` for values that might be non-scalar (length != 1 checks) since they could be vectors/lists. Use the raw value for scalar checks where the value is guaranteed numeric.

**Part B: Add two new internal helpers to the end of R/validation.R:**

1. `deprecate_arg(new_val, old_val, old_name, new_name, fn_name)`:
   - If `old_val` is not NULL, issue `warning("In ", fn_name, "(): '", old_name, "' is deprecated and will be removed in a future version.\nUse '", new_name, "' instead.", call. = FALSE)`
   - If `new_val` is NULL (user only provided old), return `old_val`
   - If `new_val` is not NULL (user provided both), return `new_val` silently (new takes precedence)
   - If `old_val` is NULL, return `new_val` (no warning)
   - Add roxygen `@keywords internal` documentation

2. `resolve_horizon(horizon, default = 20L, arg_name = "horizon")`:
   - If `horizon` is NULL, return `as.integer(default)`
   - Otherwise, call existing `validate_horizon(horizon, arg_name = arg_name)` and return result
   - Add roxygen `@keywords internal` documentation with note that NULL resolves to 20 periods (econometric convention covering business cycle dynamics)

Use base `stop()` and `warning()` with `call. = FALSE` -- do NOT add cli dependency per research recommendation.
  </action>
  <verify>Source R/validation.R in an R session; confirm `deprecate_arg(NULL, "old", "old_name", "new_name", "fn")` returns "old"; confirm `resolve_horizon(NULL)` returns 20L; confirm `resolve_horizon(5)` returns 5L. Confirm `validate_horizon(-1)` error message contains "Received: -1". Confirm `validate_probability(2)` error message contains "Received: 2".</verify>
  <done>Both helpers exist, are documented with @keywords internal, and work correctly for NULL/non-NULL inputs. All validate_horizon() and validate_probability() error messages include actual received values.</done>
</task>

<task type="auto">
  <name>Task 2: Add structural validators to utils.R and update new_bsvar_post_tbl()</name>
  <files>R/utils.R</files>
  <action>
1. **Dimension assertions in set_response_dimnames():** Add at the start of the function body:
   ```r
   if (!is.array(x) || length(dim(x)) != 4L) {
     stop("In set_response_dimnames(): expected a 4-dimensional array.\n",
          "Received: ", if (is.array(x)) paste0(length(dim(x)), " dimensions") else paste0("class ", paste(class(x), collapse = ", ")),
          call. = FALSE)
   }
   ```

2. **Dimension assertions in set_time_dimnames():** Add same pattern but check for 3 dimensions.

3. **Dimension assertions in set_hd_dimnames():** Add same pattern but check for 4 dimensions.

4. **Column validation in new_bsvar_post_tbl():** Before setting the class, validate:
   - Always-required columns: `model`, `object_type`
   - If missing any, `stop("In new_bsvar_post_tbl(): missing required columns for object_type '", object_type, "'.\nExpected: model, object_type\nMissing: ", paste(missing_cols, collapse = ", "), call. = FALSE)`
   - Keep this a lightweight two-tier check (always-required only). Do NOT build a full object_type->column map -- that would be fragile and over-engineered for internal use.

5. **Update probability defaults:** Change `probability = 0.68` to `probability = 0.90` in:
   - `as_tidy_response_array()` (line 174)
   - `as_tidy_time_array()` (line 221)
   - `as_tidy_hd_array()` (line 264)

These are internal functions in utils.R -- the default change ensures consistency even when called without explicit probability.
  </action>
  <verify>Source R/utils.R; confirm `set_response_dimnames(matrix(1:4, 2, 2))` errors with "expected a 4-dimensional array"; confirm `new_bsvar_post_tbl(data.frame(x=1), "irf")` errors with "missing required columns".</verify>
  <done>All three set_*_dimnames() have dimension assertions; new_bsvar_post_tbl() validates required columns; internal probability defaults updated to 0.90.</done>
</task>

<task type="auto">
  <name>Task 3: Add model compatibility validation and update defaults in compare.R</name>
  <files>R/compare.R</files>
  <action>
1. **Add validate_model_compatibility() as a local helper** near the top of compare.R (after collect_models usage):
   ```r
   validate_model_compatibility <- function(results, fn_name) {
     if (length(results) < 2L) return(invisible(NULL))
     var_sets <- lapply(results, function(r) sort(unique(r$variable)))
     ref <- var_sets[[1]]
     for (i in seq_along(var_sets)[-1]) {
       if (!identical(var_sets[[i]], ref)) {
         stop("In ", fn_name, "(): models have incompatible variable names.\n",
              "Model 1 variables: ", paste(ref, collapse = ", "), "\n",
              "Model ", i, " variables: ", paste(var_sets[[i]], collapse = ", "),
              call. = FALSE)
       }
     }
     if ("horizon" %in% names(results[[1]])) {
       hor_sets <- lapply(results, function(r) sort(unique(r$horizon)))
       ref_h <- hor_sets[[1]]
       for (i in seq_along(hor_sets)[-1]) {
         if (!identical(hor_sets[[i]], ref_h)) {
           stop("In ", fn_name, "(): models have incompatible horizon ranges.\n",
                "Model 1 horizons: ", paste(ref_h, collapse = ", "), "\n",
                "Model ", i, " horizons: ", paste(hor_sets[[i]], collapse = ", "),
                call. = FALSE)
         }
       }
     }
     invisible(NULL)
   }
   ```

2. **Insert validation call** in each compare_* function: after `out <- lapply(...)` and before `do.call(rbind, out)`, add `validate_model_compatibility(out, "compare_irf")` (with appropriate fn_name for each). Apply to: compare_irf, compare_cdm, compare_fevd, compare_forecast, compare_hd_event, compare_restrictions (for variable check only), compare_peak_response, compare_duration_response, compare_half_life_response, compare_time_to_threshold.

3. **Update all probability defaults** from `0.68` to `0.90` in every compare_* function signature (10 occurrences).

4. **Update all horizon defaults** from `10` to `NULL` in compare_irf, compare_cdm, compare_fevd, compare_forecast, compare_peak_response, compare_duration_response, compare_half_life_response, compare_time_to_threshold (8 functions). In the function body, resolve NULL: replace `horizon = horizon` calls with `horizon = resolve_horizon(horizon)` when passing to tidy_*/response-summary functions.

5. **Add deprecation shims for variable/shock** in compare_peak_response, compare_duration_response, compare_half_life_response, compare_time_to_threshold:
   - Add `variables = NULL, shocks = NULL` as new params (before the deprecated ones)
   - Keep `variable = NULL, shock = NULL` as deprecated params
   - At function start: `variables <- deprecate_arg(variables, variable, "variable", "variables", "compare_peak_response")` and same for shocks
   - Pass `variables`/`shocks` (plural) downstream

Note: The `compare_irf`, `compare_cdm`, `compare_fevd`, `compare_forecast` do NOT have variable/shock params -- they pass everything through to tidy_* which already handles subsetting. So no shim needed there.
  </action>
  <verify>Grep for `probability = 0.68` in R/compare.R returns 0 matches. Grep for `horizon = 10` in R/compare.R returns 0 matches. Grep for `validate_model_compatibility` returns matches in each compare_* function.</verify>
  <done>All compare_* functions have model compatibility validation, probability=0.90, horizon=NULL with resolve_horizon(), and the 4 response-summary compare functions have variable/shock deprecation shims.</done>
</task>

</tasks>

<verification>
```bash
# Zero remaining 0.68 defaults in these three files
grep -c "probability = 0.68" R/validation.R R/utils.R R/compare.R
# Zero remaining horizon = 10 defaults
grep -c "horizon = 10" R/compare.R
# New helpers exist
grep -c "deprecate_arg" R/validation.R
grep -c "resolve_horizon" R/validation.R
# Dimension assertions exist
grep -c "expected a.*dimensional array" R/utils.R
# Column validation exists
grep -c "missing required columns" R/utils.R
# Model compatibility exists
grep -c "validate_model_compatibility" R/compare.R
# Received values in error messages
grep -c "Received:" R/validation.R
```
</verification>

<success_criteria>
- deprecate_arg() and resolve_horizon() helpers added to validation.R
- validate_horizon() and validate_probability() error messages include "Received: {value}" per user decision
- All three set_*_dimnames() have dimension assertions that error on wrong-dimension input
- new_bsvar_post_tbl() validates model+object_type columns exist
- All compare_* functions validate model compatibility before rbind
- Zero instances of probability=0.68 or horizon=10 in validation.R, utils.R, compare.R
- compare_peak/duration/half_life/time_to_threshold have variable/shock deprecation shims
</success_criteria>

<output>
After completion, create `.planning/phases/06-api-consistency-error-messages/06-01-SUMMARY.md`
</output>
