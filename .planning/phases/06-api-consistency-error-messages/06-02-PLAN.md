---
phase: 06-api-consistency-error-messages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - R/tidy.R
  - R/cdm.R
  - R/hypothesis.R
  - R/audit.R
  - R/representative.R
  - R/hd-event.R
  - R/v2-utils.R
  - R/aprscenario.R
  - R/plot-event.R
  - R/plot-joint-inference.R
  - R/plot-statements.R
  - R/response-summary.R
  - R/joint-inference.R
autonomous: true

must_haves:
  truths:
    - "Zero instances of probability=0.68 remain in any R source file"
    - "Zero instances of horizon=10 as default remain in any R source file"
    - "response-summary functions accept variables/shocks (plural) with deprecation shims for singular"
    - "simultaneous_irf/cdm accept variables/shocks (plural) with deprecation shims for singular"
    - "Internal hardcoded probability in v2-utils.R updated to 0.90"
  artifacts:
    - path: "R/tidy.R"
      provides: "Updated tidy_* defaults"
      contains: "probability = 0.90"
    - path: "R/response-summary.R"
      provides: "Deprecation shims for variable->variables, shock->shocks"
      contains: "deprecate_arg"
    - path: "R/joint-inference.R"
      provides: "Updated simultaneous_* defaults and deprecation shims"
      contains: "deprecate_arg"
  key_links:
    - from: "R/response-summary.R"
      to: "R/validation.R"
      via: "deprecate_arg() calls for singular->plural migration"
      pattern: "deprecate_arg"
    - from: "R/tidy.R"
      to: "R/validation.R"
      via: "resolve_horizon() for NULL default"
      pattern: "resolve_horizon"
---

<objective>
Update probability and horizon defaults across all remaining R source files, and add singular->plural deprecation shims to response-summary and joint-inference functions.

Purpose: Complete the mechanical default changes (0.68->0.90, 10->NULL) across the 13 R files not touched by Plan 01, and add deprecation shims for variable/shock->variables/shocks in the ~20 public functions identified in the research audit.

Output: All R source files have consistent defaults and deprecation-safe parameter names.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-api-consistency-error-messages/06-CONTEXT.md
@.planning/phases/06-api-consistency-error-messages/06-RESEARCH.md
@R/tidy.R
@R/response-summary.R
@R/joint-inference.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update probability and horizon defaults in tidy, cdm, hypothesis, audit, representative, hd-event, v2-utils, aprscenario, and plot files</name>
  <files>R/tidy.R, R/cdm.R, R/hypothesis.R, R/audit.R, R/representative.R, R/hd-event.R, R/v2-utils.R, R/aprscenario.R, R/plot-event.R, R/plot-joint-inference.R, R/plot-statements.R, R/joint-inference.R</files>
  <action>
**Probability default change (0.68 -> 0.90):** In every file, replace all `probability = 0.68` with `probability = 0.90` in function signatures and internal function calls. Files and approximate counts from research:
- R/tidy.R: 13 occurrences (tidy_irf.PosteriorIR, tidy_irf_model, tidy_irf.PosteriorBSVAR, tidy_cdm.*, tidy_fevd.*, tidy_forecast.*, tidy_shocks.*, tidy_hd.*)
- R/cdm.R: 2 occurrences
- R/hypothesis.R: 5 occurrences
- R/audit.R: 5 occurrences
- R/representative.R: 7 occurrences
- R/hd-event.R: 5 occurrences
- R/v2-utils.R: 3 occurrences (CRITICAL: includes hardcoded 0.68 in as_tidy_response_array call within compute_representative_draw, line ~297)
- R/aprscenario.R: 3 occurrences
- R/plot-event.R: 2 occurrences
- R/plot-joint-inference.R: 1 occurrence
- R/plot-statements.R: 2 occurrences

**Horizon default change (10 -> NULL with resolve_horizon):** In every function that has `horizon = 10` as a default parameter:
- R/tidy.R: tidy_irf_model, tidy_irf.PosteriorBSVAR (and all aliases), tidy_cdm_model, tidy_cdm.PosteriorBSVAR, tidy_fevd_model, tidy_fevd.PosteriorBSVAR, tidy_forecast_model, tidy_forecast.PosteriorBSVAR -- change default to `horizon = NULL`
- R/cdm.R: get_cdm_draws model dispatch functions -- change to `horizon = NULL`
- R/representative.R: representative_irf_model, representative_cdm_model -- change to `horizon = NULL`
- R/joint-inference.R: simultaneous_irf_model, simultaneous_cdm_model -- change to `horizon = NULL`
- R/aprscenario.R: functions with horizon=10 -- change to `horizon = NULL`
- R/v2-utils.R: get_irf_draws model dispatch -- change to `horizon = NULL`

In each function body where horizon is passed to `bsvars::compute_impulse_responses()` or similar bsvars compute function, replace with `resolve_horizon(horizon)`. Specifically, look for patterns like:
```r
bsvars::compute_impulse_responses(object, horizon = horizon, ...)
```
Change to:
```r
bsvars::compute_impulse_responses(object, horizon = resolve_horizon(horizon), ...)
```

The `resolve_horizon()` function (added in Plan 01's validation.R) converts NULL to 20L and validates non-NULL values.

**CRITICAL:** In v2-utils.R, find the hardcoded `probability = 0.68` in internal calls (not just function signatures) and update those too. The research identified `as_tidy_response_array()` call at ~line 297 and `new_representative_object()` as having hardcoded values.

After all changes, run: `grep -rn "probability = 0.68" R/` and `grep -rn "horizon = 10[^0-9]" R/` -- both must return zero matches.
  </action>
  <verify>Run `grep -rn "probability = 0.68" R/` -- expect 0 matches. Run `grep -rn "horizon = 10[^0-9]" R/` -- expect 0 matches (be careful not to match "horizon = 100" etc). Run `grep -c "resolve_horizon" R/tidy.R R/representative.R R/joint-inference.R R/v2-utils.R` -- each should have at least 1 match.</verify>
  <done>All 11 R source files have probability=0.90 and horizon=NULL defaults. Zero instances of the old defaults remain in any R/*.R file. resolve_horizon() is used wherever horizon is passed to bsvars compute functions.</done>
</task>

<task type="auto">
  <name>Task 2: Add deprecation shims for singular variable/shock in response-summary.R and joint-inference.R</name>
  <files>R/response-summary.R, R/joint-inference.R</files>
  <action>
**response-summary.R -- 4 function families, each with PosteriorIR method, PosteriorCDM method, and _model helper:**

For `peak_response`:
- `peak_response.PosteriorIR`: Change signature from `(object, variable = NULL, shock = NULL, ...)` to `(object, variables = NULL, shocks = NULL, variable = NULL, shock = NULL, ...)`. Add at start: `variables <- deprecate_arg(variables, variable, "variable", "variables", "peak_response")` and `shocks <- deprecate_arg(shocks, shock, "shock", "shocks", "peak_response")`. Pass `variables`/`shocks` downstream to `summarise_peak_draws`.
- `peak_response.PosteriorCDM`: Same pattern.
- `peak_response_model`: Same pattern (this is the shared model-dispatch function).
- `summarise_peak_draws`: Change its signature from `variable = NULL, shock = NULL` to `variables = NULL, shocks = NULL`. Update the internal call to `subset_response_draws(draws, variables = variables, shocks = shocks, horizons = NULL)` (it already uses `variables`/`shocks` for subset_response_draws -- just rename the parameter).

Apply identical pattern to `duration_response`, `half_life_response`, `time_to_threshold` and their internal summarise_* helpers.

For the internal summarise_* functions (`summarise_peak_draws`, `summarise_duration_draws`, `summarise_half_life_draws`, `summarise_threshold_draws`): rename `variable` to `variables` and `shock` to `shocks` in their signatures. These are internal so no deprecation shim needed -- just rename.

Also update the `@param` roxygen docs on the generics:
- Add `@param variables Optional response-variable subset (character or integer vector).`
- Add `@param shocks Optional shock subset (character or integer vector).`
- Add `@param variable **Deprecated.** Use \code{variables} instead.`
- Add `@param shock **Deprecated.** Use \code{shocks} instead.`

**joint-inference.R -- simultaneous_irf and simultaneous_cdm:**

For `simultaneous_irf.PosteriorIR`: Change `variable = NULL, shock = NULL` to `variables = NULL, shocks = NULL, variable = NULL, shock = NULL`. Add deprecation shim calls. Pass `variables`/`shocks` to `simultaneous_band_impl`.

Same for `simultaneous_irf_model`, `simultaneous_cdm.PosteriorCDM`, `simultaneous_cdm_model`.

For `simultaneous_band_impl`: rename `variable` to `variables` and `shock` to `shocks` in signature.

Update roxygen docs on simultaneous_irf and simultaneous_cdm generics with same deprecation @param docs.

**IMPORTANT per user decision:** hypothesis_irf and hypothesis_cdm use `variable`, `shock`, `horizon` as constraint specifiers. The research note says these accept vectors and ARE selection-style, so per the user's locked decision ("Use plural forms: `variables` and `shocks` for selection parameters"), rename these too with deprecation shims. However, these are REQUIRED arguments (no default), so the shim pattern differs slightly:
- Change signature: `variable` stays as deprecated, add `variables = NULL` as new param
- In body: `if (is.null(variables)) variables <- variable` with deprecation warning if `variable` was used
- Actually, since these are required args, use: `variables = NULL, ..., variable = NULL` and if both NULL, error. If only old provided, warn and use it.

Wait -- the research says "Recommendation: Yes, rename them" but also notes it's an open question. The user's locked decision says "Use plural forms: variables and shocks for selection parameters (since they accept vectors)." These accept vectors, so they qualify. Apply the shim.

For hypothesis_irf/hypothesis_cdm methods and their _model helpers:
- Add `variables = NULL, shocks = NULL` as new params
- Keep `variable = NULL, shock = NULL` as deprecated
- At start: resolve using deprecate_arg pattern
- If NEITHER variables nor variable provided (both NULL), do NOT error -- the downstream code may handle NULL. Check current behavior: the current code passes `variable` through to subsetting. If variable is required, the downstream will error. Keep that behavior.
  </action>
  <verify>Run `grep -n "deprecate_arg" R/response-summary.R R/joint-inference.R R/hypothesis.R` -- should show multiple matches per file. Run `grep -c "variable = NULL, shock = NULL" R/response-summary.R` -- should return 0 (all converted to variables/shocks with deprecated params). Spot-check: confirm peak_response.PosteriorIR has both `variables = NULL` and `variable = NULL` in its signature.</verify>
  <done>All 20 public functions with singular variable/shock have deprecation shims. New variables/shocks params take precedence. Old params issue warning. Internal summarise_* helpers use plural names. Roxygen @param updated with deprecation notices.</done>
</task>

</tasks>

<verification>
```bash
# Zero old defaults anywhere in R/
grep -rn "probability = 0.68" R/
grep -rn "horizon = 10[^0-9]" R/
# Deprecation shims present
grep -c "deprecate_arg" R/response-summary.R R/joint-inference.R R/hypothesis.R R/compare.R
# resolve_horizon used where needed
grep -c "resolve_horizon" R/tidy.R R/v2-utils.R R/compare.R R/representative.R
```
</verification>

<success_criteria>
- `grep -rn "probability = 0.68" R/` returns zero matches
- `grep -rn "horizon = 10" R/` returns zero matches (in function signatures)
- All response-summary functions (peak_response, duration_response, half_life_response, time_to_threshold) have variables/shocks params with deprecation shims
- All simultaneous_* functions have variables/shocks params with deprecation shims
- hypothesis_irf/hypothesis_cdm have variables/shocks params with deprecation shims
- resolve_horizon() used in all model-dispatch functions that pass horizon to bsvars compute
</success_criteria>

<output>
After completion, create `.planning/phases/06-api-consistency-error-messages/06-02-SUMMARY.md`
</output>
