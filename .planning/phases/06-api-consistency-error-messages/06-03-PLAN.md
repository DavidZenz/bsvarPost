---
phase: 06-api-consistency-error-messages
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - inst/tinytest/test_verify_tidy_intervals.R
  - inst/tinytest/test_verify_compare.R
  - inst/tinytest/test_verify_response_summary.R
  - inst/tinytest/test_joint_inference.R
  - inst/tinytest/test_plot_joint_inference.R
  - inst/tinytest/test_tidy_compare_plot.R
  - inst/tinytest/test_edge_cases.R
  - inst/tinytest/test_validation.R
  - inst/tinytest/test_api_consistency.R
  - man/
autonomous: true

must_haves:
  truths:
    - "All existing tests pass with updated defaults (913+ tests, zero failures)"
    - "New tests verify deprecation warnings fire for old singular params"
    - "New tests verify dimension assertion errors in set_*_dimnames()"
    - "New tests verify new_bsvar_post_tbl() column validation errors"
    - "New tests verify compare_* model compatibility errors"
    - "New tests verify return types are bsvar_post_tbl for tidy_*, compare_*, hypothesis_*"
    - "R CMD check passes with zero errors and zero warnings"
    - "NAMESPACE regenerated with any new exports (unlikely but verify)"
  artifacts:
    - path: "inst/tinytest/test_api_consistency.R"
      provides: "Tests for deprecation shims, validators, default changes, return types, model compatibility"
      contains: "deprecate_arg"
    - path: "inst/tinytest/test_verify_tidy_intervals.R"
      provides: "Updated verification tests with probability=0.90 or explicit 0.68"
      min_lines: 50
  key_links:
    - from: "inst/tinytest/test_api_consistency.R"
      to: "R/validation.R"
      via: "Tests call deprecate_arg and resolve_horizon"
      pattern: "deprecate_arg|resolve_horizon"
    - from: "inst/tinytest/test_api_consistency.R"
      to: "R/utils.R"
      via: "Tests trigger dimension assertion errors"
      pattern: "set_response_dimnames|new_bsvar_post_tbl"
    - from: "inst/tinytest/test_api_consistency.R"
      to: "R/compare.R"
      via: "Tests trigger model compatibility errors"
      pattern: "validate_model_compatibility"
---

<objective>
Update existing tests for new defaults, create new tests for Phase 6 validators and deprecation shims, regenerate documentation, and verify everything passes.

Purpose: The default changes (probability 0.68->0.90, horizon 10->NULL) will break tests that rely on old defaults. This plan fixes those tests and adds new tests for the validation/deprecation infrastructure added in Plans 01-02.

Output: All tests pass, new test file for API consistency, clean R CMD check.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-api-consistency-error-messages/06-CONTEXT.md
@.planning/phases/06-api-consistency-error-messages/06-RESEARCH.md
@.planning/phases/06-api-consistency-error-messages/06-01-SUMMARY.md
@.planning/phases/06-api-consistency-error-messages/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update existing tests and create API consistency test file</name>
  <files>inst/tinytest/test_verify_tidy_intervals.R, inst/tinytest/test_verify_compare.R, inst/tinytest/test_verify_response_summary.R, inst/tinytest/test_joint_inference.R, inst/tinytest/test_plot_joint_inference.R, inst/tinytest/test_tidy_compare_plot.R, inst/tinytest/test_edge_cases.R, inst/tinytest/test_validation.R, inst/tinytest/test_api_consistency.R</files>
  <action>
**Part A: Fix existing tests that use old defaults.**

For each test file containing `0.68`, decide whether to:
(a) Keep the explicit `probability = 0.68` (if the test is verifying mathematical correctness at that specific probability), OR
(b) Remove the explicit probability argument so the test uses the new 0.90 default, OR
(c) Change to `probability = 0.90` if the test is testing default behavior.

Specific files to update:

1. `test_verify_tidy_intervals.R` (6 occurrences): These tests verify credible intervals match manual quantile computation. They explicitly pass `probability = 0.68`. Keep the explicit value -- these tests verify correctness at a SPECIFIC probability, not the default. No changes needed.

2. `test_verify_compare.R` (9 occurrences): These tests verify compare_* output matches rbind of individual calls. They explicitly pass `probability = 0.68`. Keep the explicit value -- these are correctness tests. No changes needed.

3. `test_verify_response_summary.R` (2 occurrences): Explicitly passes `probability = 0.68`. Keep -- correctness test.

4. `test_joint_inference.R` (1 occurrence): `simultaneous_cdm(cdm_obj, variable = "y", shock = "shock", probability = 0.68)` -- Update to use `variables = "y", shocks = "shock"` (plural). Keep probability = 0.68 explicit since it's a correctness test. But also verify the old singular params still work (with warning).

5. `test_plot_joint_inference.R` (1 occurrence): `simultaneous_irf(irf_obj, variable = "y", shock = "shock", probability = 0.68)` -- Update to `variables = "y", shocks = "shock"`.

6. `test_tidy_compare_plot.R` (1 occurrence): `tidy_irf(post1, horizon = 2, probability = 0.68)` -- Keep explicit value.

7. `test_edge_cases.R` (1 occurrence): `validate_probability(0.68)` -- This tests the validator, keep as-is.

8. `test_validation.R` (1 occurrence): `validate_probability(0.68)` -- Keep as-is.

**Also check:** Any test that calls tidy_irf/tidy_cdm/etc WITHOUT specifying probability and then checks `lower`/`upper` columns. Those tests relied on the 0.68 default and will now get 0.90 intervals. Scan all test files for such patterns and add explicit `probability = 0.90` or `probability = 0.68` as appropriate.

Similarly, any test calling functions with `horizon = 10` implicitly (relying on default) will now get NULL->20. If tests check horizon-dependent output shapes, they may break. Scan and fix.

**Part B: Create inst/tinytest/test_api_consistency.R** with tests for Phase 6 additions:

```r
# Test deprecate_arg helper
expect_warning(
  bsvarPost:::deprecate_arg(NULL, "old_val", "old_name", "new_name", "test_fn"),
  pattern = "deprecated"
)
expect_equal(
  suppressWarnings(bsvarPost:::deprecate_arg(NULL, "old_val", "old_name", "new_name", "test_fn")),
  "old_val"
)
# New value takes precedence
expect_equal(
  bsvarPost:::deprecate_arg("new_val", NULL, "old_name", "new_name", "test_fn"),
  "new_val"
)
# Both provided: new wins, no warning
expect_equal(
  bsvarPost:::deprecate_arg("new_val", "old_val", "old_name", "new_name", "test_fn"),
  "new_val"  # but warning still fires for old_val being non-NULL
)

# Test resolve_horizon
expect_equal(bsvarPost:::resolve_horizon(NULL), 20L)
expect_equal(bsvarPost:::resolve_horizon(5), 5L)
expect_equal(bsvarPost:::resolve_horizon(NULL, default = 30L), 30L)
expect_error(bsvarPost:::resolve_horizon(-1), pattern = "non-negative")

# Test validate_horizon includes received value in error
expect_error(bsvarPost:::validate_horizon(-1), pattern = "Received: -1")
expect_error(bsvarPost:::validate_horizon(2.5), pattern = "Received: 2.5")

# Test validate_probability includes received value in error
expect_error(bsvarPost:::validate_probability(2), pattern = "Received: 2")
expect_error(bsvarPost:::validate_probability(-0.5), pattern = "Received: -0.5")

# Test dimension assertions
expect_error(
  bsvarPost:::set_response_dimnames(matrix(1:4, 2, 2)),
  pattern = "4-dimensional"
)
expect_error(
  bsvarPost:::set_time_dimnames(matrix(1:4, 2, 2)),
  pattern = "3-dimensional"
)
expect_error(
  bsvarPost:::set_hd_dimnames(matrix(1:4, 2, 2)),
  pattern = "4-dimensional"
)

# Test new_bsvar_post_tbl column validation
expect_error(
  bsvarPost:::new_bsvar_post_tbl(data.frame(x = 1), "irf"),
  pattern = "missing required columns"
)
# Valid case: has model and object_type
result <- bsvarPost:::new_bsvar_post_tbl(
  data.frame(model = "m1", object_type = "irf", variable = "x"),
  "irf"
)
expect_true(inherits(result, "bsvar_post_tbl"))

# Test validate_model_compatibility errors on incompatible variables
fake_result_1 <- data.frame(variable = c("x", "y"), value = 1:2)
fake_result_2 <- data.frame(variable = c("a", "b"), value = 3:4)
expect_error(
  bsvarPost:::validate_model_compatibility(list(fake_result_1, fake_result_2), "compare_test"),
  pattern = "incompatible variable names"
)
# Compatible variables should pass silently
fake_result_3 <- data.frame(variable = c("x", "y"), value = 5:6)
expect_silent(
  bsvarPost:::validate_model_compatibility(list(fake_result_1, fake_result_3), "compare_test")
)
# Incompatible horizons
fake_h1 <- data.frame(variable = "x", horizon = 1:3, value = 1:3)
fake_h2 <- data.frame(variable = "x", horizon = 1:5, value = 1:5)
expect_error(
  bsvarPost:::validate_model_compatibility(list(fake_h1, fake_h2), "compare_test"),
  pattern = "incompatible horizon ranges"
)

# SC3: Return types documented and consistent within function families
# Verify inherits(result, "bsvar_post_tbl") for one function from each family
# Create minimal posterior fixture for integration tests
data(us_fiscal_lsuw, package = "bsvars")
spec <- bsvars::specify_bsvar$new(us_fiscal_lsuw, p = 1)
set.seed(42)
post <- bsvars::estimate(spec, S = 5, show_progress = FALSE)

# tidy_* family: verify return type
tidy_result <- tidy_irf(post, horizon = 3, probability = 0.90)
expect_true(inherits(tidy_result, "bsvar_post_tbl"),
  info = "tidy_irf must return bsvar_post_tbl")

# compare_* family: verify return type
post2 <- bsvars::estimate(spec, S = 5, show_progress = FALSE)
compare_result <- compare_irf(post, post2, horizon = 3, probability = 0.90)
expect_true(inherits(compare_result, "bsvar_post_tbl"),
  info = "compare_irf must return bsvar_post_tbl")

# hypothesis_* family: verify return type
hyp_result <- hypothesis_irf(post, horizon = 3, probability = 0.90)
expect_true(inherits(hyp_result, "bsvar_post_tbl"),
  info = "hypothesis_irf must return bsvar_post_tbl")

# peak_response with old singular param should warn (deprecation test)
expect_warning(
  peak_response(post, horizon = 3, variable = "gdp", shock = "gdp"),
  pattern = "deprecated"
)
# peak_response with new plural param should NOT warn
pk <- peak_response(post, horizon = 3, variables = "gdp", shocks = "gdp")
expect_true(inherits(pk, "bsvar_post_tbl"))

# simultaneous_irf with old param should warn
irf_draws <- bsvars::compute_impulse_responses(post, horizon = 3)
expect_warning(
  simultaneous_irf(irf_draws, variable = "variable1", shock = "variable1"),
  pattern = "deprecated"
)
```

Adjust variable/shock names in fixture tests based on what `us_fiscal_lsuw` actually provides (likely "gdp", "spending", "revenue" or similar -- use `rownames(post$last_draw$data_matrices$Y)` to discover). The hypothesis_irf call may need variable/shock args depending on its signature -- check actual function signature and provide valid args.
  </action>
  <verify>Run `Rscript -e "tinytest::run_test_dir('inst/tinytest')"` -- expect all tests pass with zero failures. Specifically check that test_api_consistency.R contributes 20+ new passing tests.</verify>
  <done>All existing tests updated for new defaults. New test_api_consistency.R covers deprecation shims (warning on old, silent on new), resolve_horizon (NULL->20L, explicit value passthrough), dimension assertions (wrong-dim errors), column validation (missing required columns), validate_model_compatibility (incompatible variables/horizons error, compatible passes silently), return type consistency (bsvar_post_tbl for tidy_*, compare_*, hypothesis_*), and "Received:" value in validation errors.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate documentation and run R CMD check</name>
  <files>man/</files>
  <action>
1. Run `Rscript -e "devtools::document()"` to regenerate NAMESPACE and all man/*.Rd files. This picks up:
   - New @param entries for `variables`, `shocks` (plural)
   - Deprecation notices on old `variable`, `shock` params
   - Updated probability default values in @param descriptions
   - Updated horizon default values

2. Run `Rscript -e "devtools::check()"` (or `R CMD check`) to verify:
   - Zero errors
   - Zero warnings (especially no documentation mismatches)
   - Minimal notes

3. If any documentation warnings appear (e.g., mismatched arguments in Rd vs formals), fix the roxygen comments and re-run `devtools::document()`.

4. Run the full tinytest suite one more time: `Rscript -e "tinytest::run_test_dir('inst/tinytest')"` to confirm all tests pass after documentation regeneration.

5. Final verification grep:
   - `grep -rn "probability = 0.68" R/` must return 0
   - `grep -rn "horizon = 10[^0-9]" R/` must return 0 (in function defaults, not in test assertions or comments)
  </action>
  <verify>`R CMD check` reports 0 errors, 0 warnings. `tinytest::run_test_dir('inst/tinytest')` reports 0 failures. `grep -rn "probability = 0.68" R/` returns empty.</verify>
  <done>NAMESPACE and man/ pages regenerated. R CMD check clean. All tests pass. No stale defaults remain in source.</done>
</task>

</tasks>

<verification>
```bash
# All tests pass
Rscript -e "results <- tinytest::run_test_dir('inst/tinytest'); cat('Failures:', sum(!results), '\n')"
# R CMD check clean
Rscript -e "devtools::check()"
# No stale defaults
grep -rn "probability = 0.68" R/
grep -rn "horizon = 10[^0-9]" R/
```
</verification>

<success_criteria>
- All 913+ existing tests pass (with fixes for new defaults)
- 20+ new tests in test_api_consistency.R covering Phase 6 additions
- Return type assertions verify bsvar_post_tbl for tidy_*, compare_*, hypothesis_* families
- validate_model_compatibility tested with explicit expect_error for incompatible variables and horizons
- R CMD check: 0 errors, 0 warnings
- Zero instances of probability=0.68 in R/ source files
- Zero instances of horizon=10 as default in R/ source files
- NAMESPACE and man/ pages up to date
</success_criteria>

<output>
After completion, create `.planning/phases/06-api-consistency-error-messages/06-03-SUMMARY.md`
</output>
