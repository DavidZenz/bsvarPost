---
phase: 05-test-coverage-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - inst/tinytest/test_dispatch_compare.R
  - inst/tinytest/test_dispatch_sign.R
  - inst/tinytest/test_zero_coverage.R
autonomous: true

must_haves:
  truths:
    - "All 10 compare_* functions dispatch correctly for PosteriorBSVARSV"
    - "PosteriorBSVARSIGN dispatches correctly for tidy_irf, hypothesis_irf, representative_irf, compare_irf, acceptance_diagnostics"
    - "compare_forecast runs and returns expected structure"
    - "apr_gen_mats produces error when APRScenario is absent or runs when present"
    - "most_likely_admissible_cdm runs and returns expected structure"
  artifacts:
    - path: "inst/tinytest/test_dispatch_compare.R"
      provides: "S3 dispatch coverage for compare_* family with PosteriorBSVARSV"
      min_lines: 50
    - path: "inst/tinytest/test_dispatch_sign.R"
      provides: "PosteriorBSVARSIGN dispatch for key generics"
      min_lines: 40
    - path: "inst/tinytest/test_zero_coverage.R"
      provides: "Tests for exported functions with zero test references"
      min_lines: 30
  key_links:
    - from: "inst/tinytest/test_dispatch_compare.R"
      to: "R/compare.R"
      via: "S3 dispatch for PosteriorBSVARSV"
      pattern: "compare_.*post_sv"
    - from: "inst/tinytest/test_dispatch_sign.R"
      to: "R/tidy.R"
      via: "S3 dispatch for PosteriorBSVARSIGN"
      pattern: "tidy_.*post_sign"
    - from: "inst/tinytest/test_zero_coverage.R"
      to: "R/compare.R"
      via: "compare_forecast function call"
      pattern: "compare_forecast"
---

<objective>
Test S3 dispatch for the compare_* function family with PosteriorBSVARSV, verify PosteriorBSVARSIGN dispatch across key generic families, and cover the 3 exported functions with zero test references (compare_forecast, apr_gen_mats, most_likely_admissible_cdm).

Purpose: Close the compare family dispatch gap and eliminate zero-coverage exported functions.
Output: 3 test files covering compare dispatch, SIGN dispatch, and zero-reference functions.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-test-coverage-expansion/05-RESEARCH.md
@inst/tinytest/test_compare_hd_event.R
@inst/tinytest/test_compare_response_summary.R
@R/compare.R
@R/representative.R
@R/aprscenario.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create compare_* dispatch tests and zero-reference function tests</name>
  <files>inst/tinytest/test_dispatch_compare.R, inst/tinytest/test_zero_coverage.R</files>
  <action>
**File 1: test_dispatch_compare.R** -- S3 dispatch for all compare_* functions with PosteriorBSVARSV.

Fixture setup:
- Sys.setenv(KMP_DUPLICATE_LIB_OK = "TRUE")
- data(us_fiscal_lsuw, package = "bsvars")
- set.seed(1)
- spec_sv <- bsvars::specify_bsvar_sv$new(us_fiscal_lsuw, p = 1)
- post_sv1 <- bsvars::estimate(spec_sv, S = 5, thin = 1, show_progress = FALSE)
- post_sv2 <- bsvars::estimate(spec_sv, S = 5, thin = 1, show_progress = FALSE)

Test each compare_* function with post_sv1 and post_sv2:
1. compare_irf(base = post_sv1, alt = post_sv2, horizon = 2) -- expect bsvar_post_tbl, has "model" column with "base" and "alt" values
2. compare_cdm(base = post_sv1, alt = post_sv2, horizon = 2) -- same structure checks
3. compare_fevd(base = post_sv1, alt = post_sv2, horizon = 2) -- same
4. compare_forecast(base = post_sv1, alt = post_sv2, horizon = 2) -- same (THIS IS A ZERO-REFERENCE FUNCTION)
5. compare_hd_event(base = post_sv1, alt = post_sv2, ...) -- check the function signature first in R/compare.R for required args, then test with appropriate args
6. compare_peak_response(base = post_sv1, alt = post_sv2, horizon = 2) -- same
7. compare_duration_response(base = post_sv1, alt = post_sv2, horizon = 2) -- same
8. compare_half_life_response(base = post_sv1, alt = post_sv2, horizon = 2) -- same
9. compare_time_to_threshold(base = post_sv1, alt = post_sv2, horizon = 2, threshold = 0) -- same
10. compare_restrictions -- only works with PosteriorBSVARSIGN, skip here (tested in test_dispatch_sign.R)

For each: verify inherits(result, "bsvar_post_tbl") or appropriate class, nrow > 0, "model" column present with both "base" and "alt".

**File 2: test_zero_coverage.R** -- Tests for the 3 zero-reference exported functions.

Fixture: reuse bsvars base posterior (same as above pattern).

1. compare_forecast: Already tested in test_dispatch_compare.R above. Here add deeper structural checks -- verify expected columns (variable, horizon, median, lower, upper, model).

2. most_likely_admissible_cdm(post, horizon = 2): Create PosteriorBSVAR fixture (cheaper than SV). Verify returns a list with expected components (representative_draw, draw_index, method). Test that method is "most_likely_admissible". Check it mirrors most_likely_admissible_irf structure.

3. apr_gen_mats: This requires APRScenario package. Test the requireNamespace error path:
   - If APRScenario is NOT available: verify the function produces an informative error when called without it (use tryCatch or expect_error pattern)
   - If APRScenario IS available: wrap in `if (requireNamespace("APRScenario", quietly = TRUE))` and test no-error execution with a simple scenario
   - Structure the APRScenario test so it gracefully handles both cases
  </action>
  <verify>Run both test files with tinytest::run_test_file() -- all pass, zero failures. Verify compare_forecast and most_likely_admissible_cdm now have test references.</verify>
  <done>All 9 testable compare_* functions dispatch for PosteriorBSVARSV. compare_forecast, most_likely_admissible_cdm, and apr_gen_mats all have dedicated tests.</done>
</task>

<task type="auto">
  <name>Task 2: Create PosteriorBSVARSIGN-specific dispatch tests</name>
  <files>inst/tinytest/test_dispatch_sign.R</files>
  <action>
Create test file verifying PosteriorBSVARSIGN dispatch for key generic families. Per user decision, bsvarSIGNs tests are core (always run).

Fixture setup:
- Sys.setenv(KMP_DUPLICATE_LIB_OK = "TRUE")
- data(optimism, package = "bsvarSIGNs")
- set.seed(1)
- sign_irf <- matrix(c(0, 1, rep(NA, 23)), 5, 5)  # 5x5 for optimism dataset
- spec_sign <- bsvarSIGNs::specify_bsvarSIGN$new(optimism * 100, p = 4, sign_irf = sign_irf)
- post_sign <- suppressMessages(bsvars::estimate(spec_sign, S = 5, thin = 1, show_progress = FALSE))
- Also create post_sign2 for compare tests.

Test PosteriorBSVARSIGN dispatch across representative generics from each family:
1. tidy_irf(post_sign, horizon = 2) -- bsvar_post_tbl, nrow > 0
2. tidy_cdm(post_sign, horizon = 2) -- bsvar_post_tbl, nrow > 0
3. tidy_fevd(post_sign, horizon = 2) -- bsvar_post_tbl, nrow > 0
4. hypothesis_irf(post_sign, horizon = 2, variable = 1, shock = 1, relation = ">") -- has posterior_prob
5. representative_irf(post_sign, horizon = 2) -- list with draw_index, method
6. compare_irf(base = post_sign, alt = post_sign2, horizon = 2) -- bsvar_post_tbl with model column
7. compare_restrictions(base = post_sign, alt = post_sign2) -- PosteriorBSVARSIGN-specific function, verify it runs
8. acceptance_diagnostics(post_sign) -- verify returns expected structure
9. restriction_audit(post_sign, horizon = 2) -- verify returns bsvar_post_tbl or expected audit structure
10. shock_ranking(post_sign, horizon = 2, variable = 1) -- verify returns expected structure

This is the critical SIGN dispatch test per user decision.
  </action>
  <verify>Run `Rscript -e "Sys.setenv(KMP_DUPLICATE_LIB_OK='TRUE'); tinytest::run_test_file('inst/tinytest/test_dispatch_sign.R')"` -- all pass, zero failures.</verify>
  <done>PosteriorBSVARSIGN dispatches correctly for tidy, hypothesis, representative, compare, acceptance_diagnostics, restriction_audit, and shock_ranking.</done>
</task>

</tasks>

<verification>
- All 3 test files execute without errors
- compare_forecast, apr_gen_mats, most_likely_admissible_cdm no longer have zero test references
- PosteriorBSVARSIGN dispatch confirmed for 10 key functions
- PosteriorBSVARSV dispatch confirmed for 9 compare_* functions
</verification>

<success_criteria>
- test_dispatch_compare.R passes for all compare_* functions on PosteriorBSVARSV
- test_zero_coverage.R covers all 3 previously-untested exports
- test_dispatch_sign.R passes for 10 PosteriorBSVARSIGN-dispatched functions
- No existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/05-test-coverage-expansion/05-02-SUMMARY.md`
</output>
