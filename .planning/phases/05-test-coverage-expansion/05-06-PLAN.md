---
phase: 05-test-coverage-expansion
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - inst/tinytest/test_dispatch_mix_msh_t.R
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "PosteriorBSVARMIX dispatches correctly through tidy_irf, hypothesis_irf, representative_irf"
    - "PosteriorBSVARMSH dispatches correctly through tidy_irf, hypothesis_irf, representative_irf"
    - "PosteriorBSVART dispatches correctly through tidy_irf, hypothesis_irf, representative_irf"
  artifacts:
    - path: "inst/tinytest/test_dispatch_mix_msh_t.R"
      provides: "Spot-check dispatch tests for remaining 3 posterior types"
      min_lines: 50
  key_links:
    - from: "inst/tinytest/test_dispatch_mix_msh_t.R"
      to: "R/tidy.R"
      via: "tidy_irf dispatch on PosteriorBSVARMIX/MSH/T"
      pattern: "tidy_irf\\("
    - from: "inst/tinytest/test_dispatch_mix_msh_t.R"
      to: "R/hypothesis.R"
      via: "hypothesis_irf dispatch on PosteriorBSVARMIX/MSH/T"
      pattern: "hypothesis_irf\\("
---

<objective>
Close Gap 1 from VERIFICATION.md: Three posterior types (PosteriorBSVARMIX, PosteriorBSVARMSH, PosteriorBSVART) have zero dispatch tests. Add spot-check tests confirming these types dispatch correctly through core generics.

Purpose: Satisfy ROADMAP SC2 requiring all 6 posterior types tested. These types are code-path aliases of PosteriorBSVAR, so spot-check structural assertions (not deep correctness) are sufficient.
Output: Single test file covering all 3 types.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@inst/tinytest/test_dispatch_tidy.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dispatch spot-check tests for MIX, MSH, and T posterior types</name>
  <files>inst/tinytest/test_dispatch_mix_msh_t.R</files>
  <action>
Create `inst/tinytest/test_dispatch_mix_msh_t.R` following the pattern in test_dispatch_tidy.R.

Structure:
1. Set `Sys.setenv(KMP_DUPLICATE_LIB_OK = "TRUE")` at top
2. Load data: `data(us_fiscal_lsuw, package = "bsvars")`
3. For each of the 3 types, create a fixture and run 3 dispatch checks:

**PosteriorBSVARMIX:**
```r
set.seed(1)
spec_mix <- bsvars::specify_bsvar_mix$new(us_fiscal_lsuw, p = 1, M = 2)
post_mix <- bsvars::estimate(spec_mix, S = 5, thin = 1, show_progress = FALSE)
```
- `tidy_irf(post_mix, horizon = 2)` -> check inherits "bsvar_post_tbl", nrow > 0, expected columns present
- `hypothesis_irf(post_mix, horizon = 2)` -> check inherits "bsvar_post_tbl", "posterior_prob" column exists
- `representative_irf(post_mix, horizon = 2)` -> check inherits "bsvar_post_tbl", "draw_index" column exists

**PosteriorBSVARMSH:**
```r
set.seed(1)
spec_msh <- bsvars::specify_bsvar_msh$new(us_fiscal_lsuw, p = 1, M = 2)
post_msh <- bsvars::estimate(spec_msh, S = 5, thin = 1, show_progress = FALSE)
```
- Same 3 dispatch checks as MIX

**PosteriorBSVART:**
```r
set.seed(1)
spec_t <- bsvars::specify_bsvar_t$new(us_fiscal_lsuw, p = 1)
post_t <- bsvars::estimate(spec_t, S = 5, thin = 1, show_progress = FALSE)
```
- Same 3 dispatch checks as MIX

For each assertion, use `info =` strings describing what is being tested (e.g., `info = "PosteriorBSVARMIX: tidy_irf dispatches and returns bsvar_post_tbl"`).

Keep assertions structural (class, nrow, column presence) — Phase 3 already verified numerical correctness for the shared code paths.

Do NOT use `library(tinytest)` — the test runner loads it. Follow the exact pattern of test_dispatch_tidy.R.
  </action>
  <verify>
Run: `KMP_DUPLICATE_LIB_OK=TRUE Rscript -e "tinytest::run_test_file('inst/tinytest/test_dispatch_mix_msh_t.R')"` from package root. All tests pass with zero failures.
  </verify>
  <done>PosteriorBSVARMIX, PosteriorBSVARMSH, and PosteriorBSVART each have passing dispatch tests for tidy_irf, hypothesis_irf, and representative_irf. All 6 posterior types now have dispatch coverage.</done>
</task>

</tasks>

<verification>
- Run test file in isolation: zero failures
- Run full suite: total test count increases, zero failures maintained
- grep test file for all 3 type names confirms coverage
</verification>

<success_criteria>
All 3 previously-untested posterior types (MIX, MSH, T) have at least one dispatch test each. Test file passes independently and as part of the full suite.
</success_criteria>

<output>
After completion, create `.planning/phases/05-test-coverage-expansion/05-06-SUMMARY.md`
</output>
