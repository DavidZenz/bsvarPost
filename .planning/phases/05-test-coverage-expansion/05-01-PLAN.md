---
phase: 05-test-coverage-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inst/tinytest/test_dispatch_tidy.R
  - inst/tinytest/test_dispatch_hypothesis.R
autonomous: true

must_haves:
  truths:
    - "tidy_irf, tidy_cdm, tidy_fevd, tidy_forecast, tidy_shocks, tidy_hd dispatch correctly for PosteriorBSVARSV"
    - "hypothesis_irf and hypothesis_cdm dispatch correctly for PosteriorBSVARSV"
    - "joint_hypothesis_irf and joint_hypothesis_cdm dispatch correctly for PosteriorBSVARSV"
    - "simultaneous_irf and simultaneous_cdm return expected structure for PosteriorBSVARSV"
  artifacts:
    - path: "inst/tinytest/test_dispatch_tidy.R"
      provides: "S3 dispatch coverage for all 6 tidy_* functions with PosteriorBSVARSV"
      min_lines: 40
    - path: "inst/tinytest/test_dispatch_hypothesis.R"
      provides: "S3 dispatch coverage for hypothesis_*, joint_hypothesis_*, simultaneous_* with PosteriorBSVARSV"
      min_lines: 40
  key_links:
    - from: "inst/tinytest/test_dispatch_tidy.R"
      to: "R/tidy.R"
      via: "S3 dispatch for PosteriorBSVARSV methods"
      pattern: "tidy_.*post_sv"
    - from: "inst/tinytest/test_dispatch_hypothesis.R"
      to: "R/hypothesis.R"
      via: "S3 dispatch for PosteriorBSVARSV methods"
      pattern: "hypothesis_.*post_sv"
---

<objective>
Test S3 dispatch for tidy_*, hypothesis_*, joint_hypothesis_*, and simultaneous_* function families using PosteriorBSVARSV as the spot-check type. Existing tests cover PosteriorBSVAR extensively (Phases 2-3) and PosteriorBSVARSIGN in dedicated files. PosteriorBSVARSV is the recommended third type to verify dispatch works for non-default classes.

Purpose: Fill the S3 dispatch coverage gap for tidy and hypothesis families with a type other than the primary PosteriorBSVAR.
Output: 2 test files covering tidy and hypothesis dispatch for PosteriorBSVARSV.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-test-coverage-expansion/05-RESEARCH.md
@inst/tinytest/test_tidy_compare_plot.R
@inst/tinytest/test_hypothesis.R
@R/tidy.R
@R/hypothesis.R
@R/joint-inference.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PosteriorBSVARSV dispatch tests for tidy_* family</name>
  <files>inst/tinytest/test_dispatch_tidy.R</files>
  <action>
Create test file for S3 dispatch of all 6 tidy_* functions using PosteriorBSVARSV.

Fixture setup (top of file):
- Sys.setenv(KMP_DUPLICATE_LIB_OK = "TRUE")
- data(us_fiscal_lsuw, package = "bsvars")
- set.seed(1)
- spec_sv <- bsvars::specify_bsvar_sv$new(us_fiscal_lsuw, p = 1)
- post_sv <- bsvars::estimate(spec_sv, S = 5, thin = 1, show_progress = FALSE)

Test each tidy_* function with post_sv:
1. tidy_irf(post_sv, horizon = 2) -- expect bsvar_post_tbl, nrow > 0, correct columns (variable, shock, horizon, median, lower, upper)
2. tidy_cdm(post_sv, horizon = 2) -- expect bsvar_post_tbl, nrow > 0
3. tidy_fevd(post_sv, horizon = 2) -- expect bsvar_post_tbl, nrow > 0
4. tidy_forecast(post_sv, horizon = 2) -- expect bsvar_post_tbl, nrow > 0
5. tidy_shocks(post_sv) -- expect bsvar_post_tbl, nrow > 0
6. tidy_hd(post_sv) -- expect bsvar_post_tbl, nrow > 0

For each: verify inherits(result, "bsvar_post_tbl"), nrow(result) > 0, and key columns present.

Follow existing test_tidy_compare_plot.R pattern for assertion style.
  </action>
  <verify>Run `Rscript -e "Sys.setenv(KMP_DUPLICATE_LIB_OK='TRUE'); tinytest::run_test_file('inst/tinytest/test_dispatch_tidy.R')"` from package root -- all tests pass, zero failures.</verify>
  <done>All 6 tidy_* functions dispatch correctly for PosteriorBSVARSV with structural assertions passing.</done>
</task>

<task type="auto">
  <name>Task 2: Create PosteriorBSVARSV dispatch tests for hypothesis and joint-inference families</name>
  <files>inst/tinytest/test_dispatch_hypothesis.R</files>
  <action>
Create test file for S3 dispatch of hypothesis_*, joint_hypothesis_*, and simultaneous_* using PosteriorBSVARSV.

Fixture setup (top of file):
- Sys.setenv(KMP_DUPLICATE_LIB_OK = "TRUE")
- data(us_fiscal_lsuw, package = "bsvars")
- set.seed(1)
- spec_sv <- bsvars::specify_bsvar_sv$new(us_fiscal_lsuw, p = 1)
- post_sv <- bsvars::estimate(spec_sv, S = 5, thin = 1, show_progress = FALSE)

Test each function with post_sv:
1. hypothesis_irf(post_sv, horizon = 2, variable = 1, shock = 1, relation = ">") -- expect data.frame with posterior_prob column, value between 0 and 1
2. hypothesis_cdm(post_sv, horizon = 2, variable = 1, shock = 1, relation = ">") -- expect data.frame with posterior_prob column
3. joint_hypothesis_irf(post_sv, horizon = 2) -- expect list with expected components
4. joint_hypothesis_cdm(post_sv, horizon = 2) -- expect list with expected components
5. simultaneous_irf(post_sv, horizon = 2) -- expect bsvar_post_tbl, nrow > 0, has simultaneous_prob or critical_value column
6. simultaneous_cdm(post_sv, horizon = 2) -- expect bsvar_post_tbl, nrow > 0

For each: verify the function runs without error and returns expected class/structure. Do NOT deeply verify numerical values (Phase 3 already covers that for PosteriorBSVAR).

Follow existing test_hypothesis.R pattern for assertion style.
  </action>
  <verify>Run `Rscript -e "Sys.setenv(KMP_DUPLICATE_LIB_OK='TRUE'); tinytest::run_test_file('inst/tinytest/test_dispatch_hypothesis.R')"` from package root -- all tests pass, zero failures.</verify>
  <done>hypothesis_irf, hypothesis_cdm, joint_hypothesis_irf, joint_hypothesis_cdm, simultaneous_irf, simultaneous_cdm all dispatch correctly for PosteriorBSVARSV.</done>
</task>

</tasks>

<verification>
- Both test files execute without errors
- All assertions pass (zero failures in tinytest output)
- PosteriorBSVARSV dispatch confirmed for 12 functions across tidy and hypothesis families
</verification>

<success_criteria>
- test_dispatch_tidy.R passes with assertions for all 6 tidy_* functions on PosteriorBSVARSV
- test_dispatch_hypothesis.R passes with assertions for all 6 hypothesis/joint/simultaneous functions on PosteriorBSVARSV
- No existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/05-test-coverage-expansion/05-01-SUMMARY.md`
</output>
