---
phase: 05-test-coverage-expansion
plan: 05
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03", "05-04"]
files_modified:
  - inst/tinytest/test_coverage_gaps.R
autonomous: true

must_haves:
  truths:
    - "covr::package_coverage(type='tests') reports >= 80% line coverage"
    - "Every exported function has at least one test reference"
    - "All tests pass with _R_CHECK_FORCE_SUGGESTS_=false"
    - "Zero test failures in full test suite run"
  artifacts:
    - path: "inst/tinytest/test_coverage_gaps.R"
      provides: "Targeted tests for any remaining coverage gaps found by covr"
      min_lines: 1
  key_links:
    - from: "inst/tinytest/test_coverage_gaps.R"
      to: "R/*.R"
      via: "Coverage gap filling based on covr::zero_coverage() output"
      pattern: "expect_true|expect_equal"
---

<objective>
Run covr::package_coverage() to measure actual line coverage after Plans 01-04, identify any remaining gaps below 80%, write targeted tests to close them, then validate that all tests pass under _R_CHECK_FORCE_SUGGESTS_=false.

Purpose: Verification gate per user decision -- covr >= 80% and FORCE_SUGGESTS=false compliance.
Output: Coverage measurement results, optional gap-filling test file, and FORCE_SUGGESTS validation.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-test-coverage-expansion/05-RESEARCH.md
@.planning/phases/05-test-coverage-expansion/05-01-SUMMARY.md
@.planning/phases/05-test-coverage-expansion/05-02-SUMMARY.md
@.planning/phases/05-test-coverage-expansion/05-03-SUMMARY.md
@.planning/phases/05-test-coverage-expansion/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run covr baseline, identify gaps, and fill to 80%</name>
  <files>inst/tinytest/test_coverage_gaps.R</files>
  <action>
**Step 1: Measure coverage**

Run from package root:
```r
Sys.setenv(KMP_DUPLICATE_LIB_OK = "TRUE")
cov <- covr::package_coverage(type = "tests")
pct <- covr::percent_coverage(cov)
cat("Overall coverage:", round(pct, 1), "%\n")
```

Record the overall percentage.

**Step 2: If coverage >= 80%, skip to Step 3.**

**Step 2b: If coverage < 80%, identify gaps:**
```r
zeros <- covr::zero_coverage(cov)
# Group by filename, find files with most uncovered lines
```

Write targeted tests in test_coverage_gaps.R to cover the largest gap files. Focus on:
- Calling uncovered branches/functions with appropriate inputs
- Testing error paths that validation should trigger
- Exercising conditional branches (if/else in source code)

Prioritize by file size (largest uncovered files from research: response-summary.R 605 lines, reporting.R 456 lines, v2-utils.R 423 lines, audit.R 389 lines, joint-inference.R 336 lines).

For each gap: write the minimal test that exercises the uncovered line. No-error execution tests are sufficient for plot/style code.

Re-run covr after writing gap tests to confirm >= 80%.

**Step 3: Verify every exported function has test coverage**

```r
# Get all exports
ns <- getNamespaceExports("bsvarPost")
# For each, grep test files to confirm at least one reference
```

If any export still has zero test references, add a minimal test to test_coverage_gaps.R.

**Step 4: Validate FORCE_SUGGESTS=false compliance**

Run the full test suite with FORCE_SUGGESTS=false:
```bash
_R_CHECK_FORCE_SUGGESTS_=false Rscript -e "Sys.setenv(KMP_DUPLICATE_LIB_OK='TRUE'); results <- tinytest::run_test_dir('inst/tinytest'); cat('Failures:', sum(!results), '/', length(results), '\n')"
```

All tests must pass (zero failures). Optional package tests should exit cleanly via exit_file().

If test_coverage_gaps.R ends up empty (coverage already >= 80% and all exports covered), create a minimal file with a comment explaining why and one assertion that the package loads:
```r
expect_true(requireNamespace("bsvarPost", quietly = TRUE))
```
  </action>
  <verify>
1. covr::percent_coverage() >= 80.0
2. All exported functions have at least one test reference
3. Full test suite passes with _R_CHECK_FORCE_SUGGESTS_=false (zero failures)
  </verify>
  <done>Coverage >= 80% confirmed by covr. All 66 exported functions have test coverage. Full test suite passes under FORCE_SUGGESTS=false.</done>
</task>

</tasks>

<verification>
- covr::package_coverage(type = "tests") reports >= 80% line coverage
- Every exported function in NAMESPACE has at least one test reference
- tinytest::run_test_dir("inst/tinytest") with _R_CHECK_FORCE_SUGGESTS_=false produces zero failures
- Total test count has grown from 283+ baseline
</verification>

<success_criteria>
- covr line coverage >= 80%
- Zero exported functions without test references
- Zero test failures under FORCE_SUGGESTS=false
- Coverage measurement command and result documented in SUMMARY
</success_criteria>

<output>
After completion, create `.planning/phases/05-test-coverage-expansion/05-05-SUMMARY.md`
</output>
