---
phase: 05-test-coverage-expansion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - inst/tinytest/test_dispatch_representative.R
  - inst/tinytest/test_dispatch_response_summary.R
  - inst/tinytest/test_utils_coverage.R
autonomous: true

must_haves:
  truths:
    - "representative_irf, representative_cdm, median_target_irf, median_target_cdm dispatch for PosteriorBSVARSV"
    - "peak_response, duration_response, half_life_response, time_to_threshold dispatch for PosteriorBSVARSV"
    - "Internal utility functions (new_bsvar_post_tbl, ensure_model_names, resolve_array_dimnames) are exercised"
    - "Plot styling functions (style_bsvar_plot, template_bsvar_plot, annotate_bsvar_plot, publish_bsvar_plot, theme_bsvarpost) execute without error"
  artifacts:
    - path: "inst/tinytest/test_dispatch_representative.R"
      provides: "S3 dispatch for representative_*, median_target_* with PosteriorBSVARSV"
      min_lines: 30
    - path: "inst/tinytest/test_dispatch_response_summary.R"
      provides: "S3 dispatch for response summary functions with PosteriorBSVARSV"
      min_lines: 40
    - path: "inst/tinytest/test_utils_coverage.R"
      provides: "Coverage for internal utilities and plot styling functions"
      min_lines: 50
  key_links:
    - from: "inst/tinytest/test_dispatch_representative.R"
      to: "R/representative.R"
      via: "S3 dispatch for PosteriorBSVARSV"
      pattern: "representative_.*post_sv"
    - from: "inst/tinytest/test_dispatch_response_summary.R"
      to: "R/response-summary.R"
      via: "S3 dispatch for PosteriorBSVARSV"
      pattern: "peak_response.*post_sv"
    - from: "inst/tinytest/test_utils_coverage.R"
      to: "R/utils.R"
      via: "Internal utility function calls"
      pattern: "new_bsvar_post_tbl|theme_bsvarpost"
---

<objective>
Test S3 dispatch for representative_*, median_target_*, and response summary function families with PosteriorBSVARSV. Also cover internal utility functions and plot styling functions that likely have low line coverage.

Purpose: Fill dispatch gaps for representative and response-summary families, plus cover utility/style code per user decision (no files excluded from coverage).
Output: 3 test files covering representative dispatch, response summary dispatch, and utils/plot-style coverage.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-test-coverage-expansion/05-RESEARCH.md
@inst/tinytest/test_representative.R
@inst/tinytest/test_response_summary.R
@inst/tinytest/test_plot_style.R
@R/representative.R
@R/response-summary.R
@R/utils.R
@R/plot-style.R
@R/plot-publication.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PosteriorBSVARSV dispatch tests for representative and response-summary families</name>
  <files>inst/tinytest/test_dispatch_representative.R, inst/tinytest/test_dispatch_response_summary.R</files>
  <action>
**File 1: test_dispatch_representative.R** -- S3 dispatch for representative_*, median_target_* with PosteriorBSVARSV.

Fixture setup:
- Sys.setenv(KMP_DUPLICATE_LIB_OK = "TRUE")
- data(us_fiscal_lsuw, package = "bsvars")
- set.seed(1)
- spec_sv <- bsvars::specify_bsvar_sv$new(us_fiscal_lsuw, p = 1)
- post_sv <- bsvars::estimate(spec_sv, S = 5, thin = 1, show_progress = FALSE)

Test each function:
1. representative_irf(post_sv, horizon = 2) -- expect list with representative_draw, draw_index, method components
2. representative_cdm(post_sv, horizon = 2) -- same structure
3. median_target_irf(post_sv, horizon = 2) -- expect list with draw_index, method = "median_target"
4. median_target_cdm(post_sv, horizon = 2) -- same structure

For each: verify is.list(result), has "draw_index" component, has "method" component, method matches expected value.

**File 2: test_dispatch_response_summary.R** -- S3 dispatch for peak/duration/half_life/time_to_threshold with PosteriorBSVARSV.

Fixture setup: Same as above (reuse spec_sv/post_sv pattern).

Test each function:
1. peak_response(post_sv, horizon = 2) -- expect bsvar_post_tbl or data.frame, nrow > 0
2. duration_response(post_sv, horizon = 2) -- same
3. half_life_response(post_sv, horizon = 2) -- same
4. time_to_threshold(post_sv, horizon = 2, threshold = 0) -- same

For each: verify inherits correct class, nrow > 0, key columns present (variable, shock, plus summary statistic columns like median, mean, lower, upper).

Also test that these functions accept PosteriorIR and PosteriorCDM intermediates:
- ir <- bsvars::compute_impulse_responses(post_sv, horizon = 2)
- peak_response(ir) -- should work via PosteriorIR method
- cdm_obj <- cdm(post_sv, horizon = 2)
- duration_response(cdm_obj) -- should work via PosteriorCDM method (check if this dispatch exists first in NAMESPACE)
  </action>
  <verify>Run both test files with tinytest::run_test_file() -- all pass, zero failures.</verify>
  <done>representative_irf, representative_cdm, median_target_irf, median_target_cdm, peak_response, duration_response, half_life_response, time_to_threshold all dispatch correctly for PosteriorBSVARSV. PosteriorIR/PosteriorCDM intermediate dispatch also verified where methods exist.</done>
</task>

<task type="auto">
  <name>Task 2: Create utility and plot styling coverage tests</name>
  <files>inst/tinytest/test_utils_coverage.R</files>
  <action>
Create test file covering internal utilities and plot styling functions that are likely uncovered.

Fixture setup:
- Sys.setenv(KMP_DUPLICATE_LIB_OK = "TRUE")
- data(us_fiscal_lsuw, package = "bsvars")
- set.seed(1)
- spec <- bsvars::specify_bsvar$new(us_fiscal_lsuw, p = 1)
- post <- bsvars::estimate(spec, S = 5, thin = 1, show_progress = FALSE)

**Plot styling functions (no-error execution tests per user decision):**
1. theme_bsvarpost() -- expect inherits(result, "theme"), inherits(result, "gg")
2. Create an IRF tidy table, then:
   - p <- ggplot2::autoplot(tidy_irf(post, horizon = 2))
   - styled <- style_bsvar_plot(p, preset = "paper") -- expect inherits(styled, "ggplot")
   - styled2 <- style_bsvar_plot(p, preset = "slides") -- expect inherits(styled2, "ggplot")
   - tmpl <- template_bsvar_plot(p) -- expect inherits(tmpl, "ggplot")
   - ann <- annotate_bsvar_plot(p, title = "Test", subtitle = "Sub") -- expect inherits(ann, "ggplot")
   - pub <- publish_bsvar_plot(p) -- expect inherits(pub, "ggplot")

**Internal utility functions (test via their effects):**
3. Test new_bsvar_post_tbl indirectly: tidy_irf output should have class c("bsvar_post_tbl", "tbl_df", "tbl", "data.frame") -- verify full class vector
4. Test that tidy outputs have "object_type" attribute: attr(tidy_irf(post, horizon=2), "object_type") == "irf"
5. Test model name assignment: result$model column should be "posterior" (default single-model name)

**Plot function no-error tests (covering plot-*.R files):**
6. plot_hypothesis(hypothesis_irf(post, horizon=2, variable=1, shock=1, relation=">")) -- expect inherits ggplot
7. plot_shock_ranking -- if applicable with PosteriorBSVAR, test; otherwise note SIGN-only

For plot tests: do NOT open devices. Assign to variable (p <- ...) and check class only.
  </action>
  <verify>Run `Rscript -e "Sys.setenv(KMP_DUPLICATE_LIB_OK='TRUE'); tinytest::run_test_file('inst/tinytest/test_utils_coverage.R')"` -- all pass.</verify>
  <done>Plot styling functions (style, template, annotate, publish, theme) execute without error. Internal utility function outputs verified (class vectors, attributes, model names). Plot functions produce ggplot objects.</done>
</task>

</tasks>

<verification>
- All 3 test files execute without errors
- representative and response-summary dispatch confirmed for PosteriorBSVARSV
- Plot styling and utility functions have non-zero coverage
- No existing tests broken
</verification>

<success_criteria>
- test_dispatch_representative.R passes for 4 representative/median_target functions on PosteriorBSVARSV
- test_dispatch_response_summary.R passes for 4 response summary functions on PosteriorBSVARSV + PosteriorIR/PosteriorCDM where applicable
- test_utils_coverage.R passes for all plot styling functions and utility checks
- No existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/05-test-coverage-expansion/05-03-SUMMARY.md`
</output>
