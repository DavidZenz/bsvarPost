---
phase: 07-vignettes-multi-platform
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/render-vignette-figures.R
  - vignettes/fixtures/fiscal_post_bsvar.rds
  - vignettes/fixtures/fiscal_post_bsvar_alt.rds
  - vignettes/figures/cdm-showcase.png
  - vignettes/figures/compare-irf-showcase.png
  - vignettes/figures/representative-showcase.png
  - vignettes/figures/diagnostics-showcase.png
  - vignettes/figures/hd-event-showcase.png
  - vignettes/figures/hypothesis-showcase.png
  - .github/workflows/R-CMD-check.yaml
autonomous: true

must_haves:
  truths:
    - "Pre-computed .rds fixture files exist in vignettes/fixtures/"
    - "Pre-rendered figure PNGs exist in vignettes/figures/ including hypothesis-showcase.png"
    - "CI workflow tests 5 OS/R combinations (macos/windows/ubuntu x release/devel/oldrel)"
    - "Fixture generation script runs without errors on macOS"
  artifacts:
    - path: "tools/render-vignette-figures.R"
      provides: "Fixture generation and figure rendering"
      contains: "saveRDS.*compress.*xz"
    - path: "vignettes/fixtures/fiscal_post_bsvar.rds"
      provides: "Primary posterior fixture from us_fiscal_lsuw"
    - path: "vignettes/fixtures/fiscal_post_bsvar_alt.rds"
      provides: "Alternative specification posterior fixture"
    - path: ".github/workflows/R-CMD-check.yaml"
      provides: "Multi-platform CI matrix"
      contains: "matrix"
  key_links:
    - from: "tools/render-vignette-figures.R"
      to: "vignettes/fixtures/*.rds"
      via: "saveRDS with compress=xz"
      pattern: "saveRDS.*fixtures"
    - from: ".github/workflows/R-CMD-check.yaml"
      to: "R CMD check"
      via: "r-lib/actions v2 matrix strategy"
      pattern: "setup-pandoc"
---

<objective>
Create pre-computed .rds fixtures and pre-rendered figures for all 3 vignettes, and expand the CI matrix to 5 platform/R combinations.

Purpose: Vignettes need pre-loaded posterior objects so R CMD check avoids running estimate(). CI must validate across all CRAN-targeted platforms before submission.
Output: .rds fixture files, updated showcase figures, and multi-platform CI workflow.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-vignettes-multi-platform/07-RESEARCH.md
@tools/render-vignette-figures.R
@.github/workflows/R-CMD-check.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update fixture generation script and generate .rds fixtures + figures</name>
  <files>
    tools/render-vignette-figures.R
    vignettes/fixtures/fiscal_post_bsvar.rds
    vignettes/fixtures/fiscal_post_bsvar_alt.rds
    vignettes/figures/cdm-showcase.png
    vignettes/figures/compare-irf-showcase.png
    vignettes/figures/representative-showcase.png
    vignettes/figures/diagnostics-showcase.png
    vignettes/figures/hd-event-showcase.png
    vignettes/figures/hypothesis-showcase.png
  </files>
  <action>
Rewrite `tools/render-vignette-figures.R` to:

1. Add macOS OpenMP workaround at the top:
   ```r
   if (Sys.info()[["sysname"]] == "Darwin") {
     Sys.setenv(KMP_DUPLICATE_LIB_OK = "TRUE")
   }
   ```

2. Keep the existing `.save_gg()` and `.save_base()` helper functions.

3. Generate two .rds fixture files in `vignettes/fixtures/`:
   - `fiscal_post_bsvar.rds`: `specify_bsvar$new(us_fiscal_lsuw, p = 1)` estimated with `S = 200, thin = 1, set.seed(123)`. Save with `compress = "xz"`.
   - `fiscal_post_bsvar_alt.rds`: `specify_bsvar$new(us_fiscal_lsuw, p = 3)` estimated with `S = 200, thin = 1, set.seed(456)`. Save with `compress = "xz"`.
   - Print `colnames(us_fiscal_lsuw)` so the economic narrative can reference actual variable names.
   - Print file sizes of both .rds files to verify they are under 2MB total.

4. Regenerate all existing showcase figures using the updated API:
   - `cdm-showcase.png`: CDM from fiscal_post_bsvar, horizon = 20 (updated from 8 per new default)
   - `compare-irf-showcase.png`: `compare_irf(baseline = post_bsvar, alternative = post_bsvar_alt, horizon = 20)` -- use `baseline =` / `alternative =` naming instead of old `p1 =` / `p2 =`
   - `representative-showcase.png`: `median_target_irf(post_bsvar, horizon = 20)`
   - `diagnostics-showcase.png`: Uses bsvarSIGNs `optimism` data (keep existing pattern but update to S = 200)
   - `hd-event-showcase.png`: Uses bsvarSIGNs `optimism` data (keep existing pattern)

5. Add a new figure generation block for the hypothesis testing vignette:
   - `hypothesis-showcase.png`: `plot_hypothesis(post_bsvar, type = "irf", variables = 1, shocks = 1, horizon = 0:20, relation = ">", value = 0)` -- use the fiscal model, plural API params

6. Use the new .rds fixtures (loaded via readRDS) for all bsvar-based figure generation instead of re-estimating. Keep separate bsvarSIGNs estimation for sign-restriction figures (diagnostics, hd-event) since those need PosteriorBSVARSIGN objects.

After rewriting the script, run it:
```bash
cd /Users/davidzenz/.claude-worktrees/bsvarPost/sweet-chatelet && Rscript tools/render-vignette-figures.R
```

Verify:
- Both .rds files exist in `vignettes/fixtures/`
- All 6 PNG files exist in `vignettes/figures/`
- Total .rds size is under 3MB
  </action>
  <verify>
```bash
ls -la vignettes/fixtures/*.rds && ls -la vignettes/figures/*.png && du -sh vignettes/fixtures/
```
Both .rds files exist, all 6 PNG files exist, total fixtures directory under 3MB.
  </verify>
  <done>Two .rds fixture files and 6 showcase PNGs generated from updated API with plural params and current defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Expand CI workflow to 5-platform matrix</name>
  <files>.github/workflows/R-CMD-check.yaml</files>
  <action>
Replace the existing single-ubuntu `.github/workflows/R-CMD-check.yaml` with the standard r-lib/actions v2 check-standard matrix. The new workflow must include:

1. Matrix strategy with `fail-fast: false` and 5 configurations:
   - `{os: macos-latest, r: 'release'}`
   - `{os: windows-latest, r: 'release'}`
   - `{os: ubuntu-latest, r: 'devel', http-user-agent: 'release'}`
   - `{os: ubuntu-latest, r: 'release'}`
   - `{os: ubuntu-latest, r: 'oldrel-1'}`

2. Environment variables at job level:
   - `GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}`
   - `R_KEEP_PKG_SOURCE: yes`
   - `_R_CHECK_FORCE_SUGGESTS_: false`

3. Steps in order:
   a. `actions/checkout@v4`
   b. `r-lib/actions/setup-pandoc@v2` (CRITICAL: without this, vignette Rmd rendering fails on Windows/macOS)
   c. `r-lib/actions/setup-r@v2` with `r-version: ${{ matrix.config.r }}`, `http-user-agent: ${{ matrix.config.http-user-agent }}`, `use-public-rspm: true`
   d. `r-lib/actions/setup-r-dependencies@v2` with `extra-packages: any::rcmdcheck` and `needs: check`
   e. `r-lib/actions/check-r-package@v2` with `args: 'c("--as-cran", "--no-manual")'`, `build_args: 'character()'`, `upload-snapshots: true`

4. Keep the existing "Show check logs on failure" step and "Upload check directory" / "Upload check logs" steps, but update the artifact name to include `${{ matrix.config.os }}-${{ matrix.config.r }}` to avoid name collisions across matrix jobs.

5. Keep triggers on push to main and pull_request to main.
  </action>
  <verify>
Validate YAML syntax:
```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/R-CMD-check.yaml'))" && echo "YAML valid"
```
Confirm matrix contains 5 entries and setup-pandoc step exists:
```bash
grep -c "os:" .github/workflows/R-CMD-check.yaml  # Should be 5
grep "setup-pandoc" .github/workflows/R-CMD-check.yaml  # Should match
```
  </verify>
  <done>CI workflow uses 5-platform matrix with fail-fast: false, setup-pandoc, and _R_CHECK_FORCE_SUGGESTS_=false.</done>
</task>

</tasks>

<verification>
- `ls vignettes/fixtures/*.rds` shows 2 files
- `ls vignettes/figures/*.png` shows 6 files (including hypothesis-showcase.png)
- `du -sh vignettes/fixtures/` is under 3MB
- `.github/workflows/R-CMD-check.yaml` contains matrix with 5 OS/R configs
- YAML parses without errors
</verification>

<success_criteria>
- Pre-computed fixtures ready for vignette readRDS() calls
- All showcase figures regenerated with current API (plural params, updated defaults)
- CI matrix covers macos/windows/ubuntu with release/devel/oldrel-1
- Pandoc setup step present in CI workflow
</success_criteria>

<output>
After completion, create `.planning/phases/07-vignettes-multi-platform/07-01-SUMMARY.md`
</output>
