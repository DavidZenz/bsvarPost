---
phase: 08-cran-submission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DESCRIPTION
  - NEWS.md
  - cran-comments.md
  - inst/WORDLIST
  - .Rbuildignore
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "DESCRIPTION version is 1.0.0 with no legacy Author/Maintainer fields"
    - "NEWS.md presents bsvarPost 1.0.0 as initial CRAN release with user-facing highlights"
    - "cran-comments.md exists and is excluded from package tarball"
    - "inst/WORDLIST covers all domain-specific terms flagged by spelling check"
    - "CRAN-SUBMISSION is excluded from build and git"
  artifacts:
    - path: "DESCRIPTION"
      provides: "Clean CRAN-ready package metadata"
      contains: "Version: 1.0.0"
    - path: "NEWS.md"
      provides: "v1.0.0 changelog"
      contains: "bsvarPost 1.0.0"
    - path: "cran-comments.md"
      provides: "CRAN reviewer communication"
      contains: "Test environments"
    - path: "inst/WORDLIST"
      provides: "Domain-specific spelling exceptions"
    - path: ".Rbuildignore"
      provides: "Build exclusion rules"
      contains: "cran-comments"
  key_links:
    - from: ".Rbuildignore"
      to: "cran-comments.md"
      via: "exclusion pattern"
      pattern: "cran-comments"
    - from: "DESCRIPTION"
      to: "Authors@R"
      via: "no legacy Author/Maintainer fields"
      pattern: "Authors@R.*cph"
---

<objective>
Create all CRAN submission artifacts and fix DESCRIPTION for v1.0.0 release.

Purpose: The package needs four artifacts (NEWS.md rewrite, cran-comments.md, inst/WORDLIST, .Rbuildignore updates) and DESCRIPTION fixes (remove legacy fields, add cph, bump version) before any validation checks can run.
Output: All submission metadata files ready for the validation gauntlet in Plan 02.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cran-submission/08-RESEARCH.md
@.planning/phases/08-cran-submission/08-CONTEXT.md
@DESCRIPTION
@NEWS.md
@.Rbuildignore
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix DESCRIPTION and create NEWS.md</name>
  <files>DESCRIPTION, NEWS.md</files>
  <action>
  **DESCRIPTION fixes (4 changes):**
  1. Remove the `Author: David Zenz [aut, cre]` line entirely
  2. Remove the `Maintainer: David Zenz <office@davidzenz.com>` line entirely
  3. Update `Authors@R` to add `"cph"` role: `person("David", "Zenz", role = c("aut", "cre", "cph"), email = "office@davidzenz.com")`
  4. Update `Version: 0.2.0` to `Version: 1.0.0` and `Date: 2026-03-07` to `Date: 2026-03-13`
  5. Optionally polish the Description field to better reflect the full v1.0.0 feature set (at Claude's discretion per CONTEXT.md)

  **NEWS.md rewrite:**
  Completely rewrite NEWS.md as a fresh v1.0.0 initial CRAN release (per locked decision: NOT an upgrade from 0.2.0). Follow tidyverse NEWS style guide:
  - Level 1 heading: `# bsvarPost 1.0.0`
  - Open with: `Initial CRAN release.`
  - Use level 2 sections for grouping: `## Breaking changes and deprecations`, `## Core posterior extraction`, `## Hypothesis testing and inference`, `## Model comparison`, `## Response summaries`, `## Plotting and reporting`, etc.
  - Document deprecation shims (per locked decision): singular `variable`/`shock` params still work with warning, will be removed in future version
  - Identify notable features from Phases 1-7 of work. Key features to highlight:
    - tidy_irf(), tidy_cdm(), tidy_fevd(), tidy_shocks(), tidy_hd(), tidy_forecast() extractors
    - cdm() cross-package cumulative dynamic multiplier
    - hypothesis_irf(), hypothesis_cdm() posterior hypothesis testing
    - joint_hypothesis_irf/cdm(), simultaneous_irf/cdm() joint inference
    - representative_irf(), representative_cdm() representative model selection
    - compare_* family (irf, cdm, fevd, restrictions, peak, duration, half_life, time_to_threshold, hd_event, acceptance_diagnostics)
    - peak_response(), duration_response(), half_life_response(), time_to_threshold() response summaries
    - restriction_audit(), magnitude_audit() audit helpers
    - acceptance_diagnostics() for bsvarSIGNs
    - autoplot() support for all tidy and comparison outputs
    - theme_bsvarpost(), style_bsvar_plot(), publish_bsvar_plot() styling
    - as_gt(), as_flextable(), as_kable(), write_bsvar_csv(), report_bundle(), report_table() reporting
    - tidy_hd_event(), shock_ranking() event-study helpers
  - One bullet per user-facing change, function names in backticks with parens
  - Wrap lines at 80 characters
  - User-facing highlights only: skip internal refactoring, test additions, CI changes

  IMPORTANT: Use `KMP_DUPLICATE_LIB_OK=TRUE` prefix for any Rscript commands on macOS.
  </action>
  <verify>
  Run: `grep -c "Version: 1.0.0" DESCRIPTION` returns 1.
  Run: `grep -c "Author:" DESCRIPTION` returns 0 (legacy Author field removed).
  Run: `grep -c "Maintainer:" DESCRIPTION` returns 0 (legacy Maintainer field removed).
  Run: `grep "cph" DESCRIPTION` shows cph in Authors@R.
  Run: `head -3 NEWS.md` shows `# bsvarPost 1.0.0` and `Initial CRAN release.`
  </verify>
  <done>DESCRIPTION has version 1.0.0, date 2026-03-13, cph role, no legacy Author/Maintainer fields. NEWS.md is a complete v1.0.0 initial release changelog with user-facing highlights grouped by category.</done>
</task>

<task type="auto">
  <name>Task 2: Create inst/WORDLIST, cran-comments.md, and update build exclusions</name>
  <files>inst/WORDLIST, cran-comments.md, .Rbuildignore, .gitignore</files>
  <action>
  **Install required packages:**
  Run `KMP_DUPLICATE_LIB_OK=TRUE Rscript -e 'install.packages(c("devtools", "spelling", "urlchecker", "goodpractice"), repos="https://cloud.r-project.org")'` to ensure all validation tools are available for this plan and Plan 02.

  **inst/WORDLIST:**
  1. Create `inst/` directory if it doesn't exist
  2. Run `KMP_DUPLICATE_LIB_OK=TRUE Rscript -e 'spelling::spell_check_package(".")'` to identify all flagged terms
  3. Review output: separate real misspellings (fix in source) from domain terms (add to WORDLIST)
  4. Run `KMP_DUPLICATE_LIB_OK=TRUE Rscript -e 'spelling::update_wordlist(".")'` to auto-populate inst/WORDLIST
  5. Verify the WORDLIST contains expected domain terms: BSVAR, bsvars, bsvarSIGNs, bsvarPost, Bayesian, MCMC, VAR, IRF, CDM, FEVD, Cholesky, etc.
  6. If update_wordlist() is interactive and doesn't work non-interactively, create inst/WORDLIST manually from spell_check_package() output, listing one term per line sorted alphabetically

  **cran-comments.md:**
  Create cran-comments.md in the package root with standard CRAN format:
  ```
  ## Test environments

  * macOS (arm64), R 4.x.x -- local R CMD check --as-cran
  * GitHub Actions: macOS/release, windows/release, ubuntu/devel, ubuntu/release, ubuntu/oldrel-1

  ## R CMD check results

  0 errors | 0 warnings | 1 note

  NOTE: New submission

  This is the first CRAN submission of bsvarPost. The "New submission" NOTE
  is expected for first-time packages.

  ## Downstream dependencies

  There are currently no downstream dependencies on bsvarPost.
  ```
  Fill in actual R version from `R --version`. Will be updated in Plan 02 after validation runs confirm results. If WinBuilder results arrive, add that as a test environment too.

  **.Rbuildignore updates:**
  Add these patterns (if not already present):
  - `^cran-comments\.md$`
  - `^CRAN-SUBMISSION$`

  **.gitignore updates:**
  Add (if not already present):
  - `CRAN-SUBMISSION`
  </action>
  <verify>
  Run: `test -f inst/WORDLIST && echo "exists" || echo "missing"` returns "exists".
  Run: `test -f cran-comments.md && echo "exists" || echo "missing"` returns "exists".
  Run: `grep "cran-comments" .Rbuildignore` returns a match.
  Run: `grep "CRAN-SUBMISSION" .Rbuildignore` returns a match.
  Run: `grep "CRAN-SUBMISSION" .gitignore` returns a match.
  Run: `KMP_DUPLICATE_LIB_OK=TRUE Rscript -e 'cat(length(spelling::spell_check_package(".")$word), "words flagged\n")'` returns 0 words flagged.
  </verify>
  <done>inst/WORDLIST exists with domain-specific terms and spelling check passes cleanly. cran-comments.md exists with standard CRAN format. .Rbuildignore excludes cran-comments.md and CRAN-SUBMISSION. .gitignore excludes CRAN-SUBMISSION.</done>
</task>

</tasks>

<verification>
- DESCRIPTION version is 1.0.0 with proper Authors@R (cph role, no legacy fields)
- NEWS.md is a complete v1.0.0 changelog following tidyverse style
- cran-comments.md exists and documents check results
- inst/WORDLIST exists and spelling::spell_check_package() returns 0 flagged words
- .Rbuildignore and .gitignore properly exclude submission artifacts
- All required validation packages (devtools, spelling, urlchecker, goodpractice) are installed
</verification>

<success_criteria>
All four CRAN submission artifacts exist and are properly configured. DESCRIPTION is clean for CRAN submission. Validation packages are installed and ready for Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/08-cran-submission/08-01-SUMMARY.md`
</output>
