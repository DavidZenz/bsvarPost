---
phase: 08-cran-submission
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Package tarball submitted to CRAN via devtools::release()"
    - "User is aware they must check email for CRAN confirmation link"
  artifacts: []
  key_links:
    - from: "devtools::release()"
      to: "CRAN"
      via: "package submission"
      pattern: "release"
---

<objective>
Submit bsvarPost 1.0.0 to CRAN via devtools::release().

Purpose: Complete the final submission step. devtools::release() is interactive and requires human attention for randomized confirmation questions.
Output: Package submitted to CRAN; user monitors email for reviewer correspondence.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cran-submission/08-02-SUMMARY.md
@cran-comments.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify submission readiness</name>
  <files></files>
  <action>
  Present the user with a summary of all completed work from Plans 01 and 02:
  - DESCRIPTION: v1.0.0, cph role, no legacy Author/Maintainer fields
  - NEWS.md: Complete v1.0.0 initial release changelog
  - cran-comments.md: Documents platform check results
  - inst/WORDLIST: Domain-specific spelling exceptions
  - Validation: spelling clean, URLs valid, goodpractice reviewed, R CMD check --as-cran passes

  Ask user to review NEWS.md and cran-comments.md before proceeding.
  If WinBuilder email arrived at office@davidzenz.com, ask user to share results so cran-comments.md can be updated.
  </action>
  <verify>User responds with "approved" or provides specific changes needed.</verify>
  <done>User has confirmed submission readiness.</done>
</task>

<task type="auto">
  <name>Task 2: Submit to CRAN via devtools::release()</name>
  <files></files>
  <action>
  Run the CRAN submission:
  ```
  KMP_DUPLICATE_LIB_OK=TRUE Rscript -e 'devtools::release(".")'
  ```

  IMPORTANT: devtools::release() is interactive. It will:
  1. Run R CMD check one final time
  2. Ask a series of yes/no questions (randomized correct answers to prevent rubber-stamping)
  3. Build and upload the tarball to CRAN
  4. Create a CRAN-SUBMISSION file in the package root

  If devtools::release() cannot run non-interactively, inform the user that they need to run it in an interactive R session:
  ```r
  setwd("/Users/davidzenz/.claude-worktrees/bsvarPost/sweet-chatelet")
  devtools::release(".")
  ```

  After submission completes:
  - Inform user to check office@davidzenz.com for CRAN confirmation email
  - User must click the confirmation link in the email to finalize submission
  - CRAN team review typically takes 1-10 business days for new packages
  - User handles any CRAN reviewer correspondence (per locked decision)
  </action>
  <verify>
  devtools::release() completes or user is informed of the interactive R command to run.
  CRAN-SUBMISSION file exists in package root (created by devtools::release()).
  </verify>
  <done>Package submitted to CRAN via devtools::release(), or user has clear instructions for the interactive submission command. User knows to check email for confirmation.</done>
</task>

</tasks>

<verification>
- devtools::release() executed or user has the exact command to run
- User informed about CRAN confirmation email requirement
- CRAN-SUBMISSION file handled (in .gitignore and .Rbuildignore)
</verification>

<success_criteria>
bsvarPost 1.0.0 is submitted to CRAN. User knows to monitor email for confirmation and reviewer correspondence.
</success_criteria>

<output>
After completion, create `.planning/phases/08-cran-submission/08-03-SUMMARY.md`
</output>
