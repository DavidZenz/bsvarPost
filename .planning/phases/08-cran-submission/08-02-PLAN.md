---
phase: 08-cran-submission
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - cran-comments.md
  - inst/WORDLIST
autonomous: true

must_haves:
  truths:
    - "spelling::spell_check_package() passes with 0 flagged words"
    - "All documentation URLs are valid (no 301 redirects, no broken links)"
    - "goodpractice::gp() reports no critical issues"
    - "R CMD check --as-cran produces 0 errors, 0 warnings"
    - "cran-comments.md reflects actual check results"
  artifacts:
    - path: "cran-comments.md"
      provides: "Updated check results for CRAN team"
      contains: "0 errors"
    - path: "inst/WORDLIST"
      provides: "Complete spelling exceptions (if any additions needed)"
  key_links:
    - from: "cran-comments.md"
      to: "R CMD check results"
      via: "documents actual check output"
      pattern: "0 errors.*0 warnings"
---

<objective>
Run the full CRAN pre-submission validation gauntlet: spelling, URL check, goodpractice, R CMD check --as-cran.

Purpose: Validate that all artifacts from Plan 01 are correct and the package passes all CRAN quality checks before submission. Fix any issues found and update cran-comments.md with actual results.
Output: Clean validation results documented in cran-comments.md; package ready for submission.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cran-submission/08-RESEARCH.md
@.planning/phases/08-cran-submission/08-01-SUMMARY.md
@DESCRIPTION
@cran-comments.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run spelling, URL, and goodpractice checks</name>
  <files>inst/WORDLIST</files>
  <action>
  Run the first three validation checks in sequence. Fix issues as they arise. All Rscript commands must use `KMP_DUPLICATE_LIB_OK=TRUE` prefix.

  **Step 1 - Spelling check:**
  ```
  KMP_DUPLICATE_LIB_OK=TRUE Rscript -e 'results <- spelling::spell_check_package("."); print(results); cat("\n", nrow(results), "words flagged\n")'
  ```
  - If any words are flagged: determine if they are real misspellings (fix in source files) or domain terms (add to inst/WORDLIST via `spelling::update_wordlist(".")` or manually)
  - Re-run spell check until 0 words flagged

  **Step 2 - URL validation:**
  ```
  KMP_DUPLICATE_LIB_OK=TRUE Rscript -e 'urlchecker::url_check(".")'
  ```
  - If 301 redirects found: run `urlchecker::url_update(".")` to fix them automatically, or update URLs manually
  - If broken links found: fix or remove the URLs in source files
  - Re-run until clean

  **Step 3 - goodpractice audit:**
  ```
  KMP_DUPLICATE_LIB_OK=TRUE Rscript -e 'gp_results <- goodpractice::gp("."); print(gp_results)'
  ```
  - Review findings. Expected: coverage above 75% threshold (currently 86.9%)
  - For critical findings: fix them
  - For acceptable findings (e.g., lintr style preferences, cyclocomp on complex functions): document in cran-comments.md if CRAN-relevant, otherwise note in SUMMARY
  - Per user decision: Claude decides whether to address or document goodpractice findings

  NOTE: goodpractice::gp() can take several minutes as it runs rcmdcheck, covr, lintr, and cyclocomp internally. Use a 10-minute timeout.
  </action>
  <verify>
  `KMP_DUPLICATE_LIB_OK=TRUE Rscript -e 'cat(nrow(spelling::spell_check_package(".")), "words flagged\n")'` returns `0 words flagged`.
  `KMP_DUPLICATE_LIB_OK=TRUE Rscript -e 'r <- urlchecker::url_check("."); print(r)'` shows no issues.
  goodpractice output reviewed and any critical issues resolved.
  </verify>
  <done>Spelling check passes with 0 flagged words. All URLs are valid. goodpractice findings reviewed and addressed or documented.</done>
</task>

<task type="auto">
  <name>Task 2: Run R CMD check --as-cran and update cran-comments.md</name>
  <files>cran-comments.md</files>
  <action>
  **Run the final CRAN-realistic check:**
  ```
  KMP_DUPLICATE_LIB_OK=TRUE Rscript -e '_R_CHECK_FORCE_SUGGESTS_=FALSE; devtools::check(remote = TRUE, manual = TRUE, env_vars = c("_R_CHECK_FORCE_SUGGESTS_" = "false"))'
  ```
  Or via command line:
  ```
  cd /Users/davidzenz/.claude-worktrees/bsvarPost/sweet-chatelet && KMP_DUPLICATE_LIB_OK=TRUE R CMD build . && KMP_DUPLICATE_LIB_OK=TRUE _R_CHECK_FORCE_SUGGESTS_=false R CMD check --as-cran bsvarPost_1.0.0.tar.gz
  ```

  Expected result: 0 errors, 0 warnings, 1 NOTE (new submission). The qpdf WARNING is local-environment-only and will NOT appear on CRAN servers.

  If any unexpected errors or warnings appear: fix the underlying issue, re-run check.

  **Update cran-comments.md:**
  Update the file with actual check results from this run:
  - Fill in exact R version from the check output
  - Confirm the 0E/0W/1N result (or update if different)
  - Note the new-submission NOTE explanation
  - Keep the downstream dependencies section

  **Attempt WinBuilder submission:**
  ```
  KMP_DUPLICATE_LIB_OK=TRUE Rscript -e 'devtools::check_win_devel(".")'
  ```
  - If this succeeds (tarball uploaded to WinBuilder): note in cran-comments.md that WinBuilder was submitted, results pending
  - If devtools::check_win_devel() fails for any reason: note in SUMMARY that manual upload to https://win-builder.r-project.org/upload.aspx is an alternative
  - WinBuilder results arrive by email to office@davidzenz.com (~30 min). Do NOT block on results. Update cran-comments.md when results arrive (user can do this before final submission).

  Use 10-minute timeout for R CMD check.
  </action>
  <verify>
  R CMD check output shows 0 errors, 0 warnings (package-level; qpdf local WARNING excluded).
  `cat cran-comments.md` shows updated check results with actual R version and platform info.
  WinBuilder submission attempted (success or failure documented).
  </verify>
  <done>R CMD check --as-cran passes with 0 errors, 0 warnings, 1 NOTE (new submission only). cran-comments.md updated with actual results. WinBuilder submission attempted.</done>
</task>

</tasks>

<verification>
- spelling::spell_check_package() returns 0 flagged words
- urlchecker::url_check() returns no issues
- goodpractice::gp() has no critical unaddressed findings
- R CMD check --as-cran: 0 errors, 0 warnings
- cran-comments.md reflects actual validation results
- WinBuilder submission attempted
</verification>

<success_criteria>
All four validation checks pass cleanly. cran-comments.md accurately documents results. Package is validated and ready for CRAN submission via devtools::release().
</success_criteria>

<output>
After completion, create `.planning/phases/08-cran-submission/08-02-SUMMARY.md`
</output>
